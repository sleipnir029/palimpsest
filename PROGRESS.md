# palimpsest progress

Append one line per merged task. Status markers: `✓` done, `⏳` in progress, `⏭` skipped, `🔁` redone.

## Week 1 — foundations

- ✓ T01 pixi init + lockfile — reproducible osx-arm64/linux-64 env; `pixi install` + `import palimpsest` verified (2026-05-26, 5404418)
- ✓ T02 repo skeleton — 7 stub modules + 5 subpackages + dir markers under `src/palimpsest/`; all imports ok (2026-05-26)
- ✓ T03 constitution + license — verified CLAUDE.md intact (6 grep checks); added MIT `LICENSE` + `AUTHORS.md` (2026-05-26)
- ✓ T04 anthropic provider — `AnthropicProvider` + `LLMResponse` wrap the Anthropic SDK (max_tokens 4096, ephemeral cache on system/tools); 2 live tests green, cache read 3203 tok. Model `claude-sonnet-4-6` per user (card said `-4-5`); in-test cost logging (CostMeter is T05) (2026-05-26)
- ✓ T05 cost meter — `CostMeter` + `BudgetExceeded` over SQLite (`cost_ledger`/`settings`, seed €50). `cap`/`soft` live-read for `/budget`; `record_llm`/`record_gpu`/`check_or_raise`/`set_budget` (refuses below spend). 5 offline tests green + persistence snippet ok. stdlib sqlite3, no new deps (2026-05-26)
- ✓ T06 agent loop — `Agent` (think→act→observe, 97 LOC) + `MaxTurnsExceeded`; module-level `TOOLS` registry + `@register` decorator. Meters each turn (check_or_raise €0.05, record_llm at Sonnet pricing ×0.92 EUR). Caches system+tools only when non-empty (empty block → 400). Logs `provider.name` (=`claude-sonnet-4-6`, not card's `-4-5`). Added `last_usage` so the cache test can read tokens. 3 tests green: no-tools→"pong", cache_read=3603 on 2nd run, max_turns raises (stub provider, deterministic) (2026-05-26)

## Week 2 — parsing & cache

- ✓ T07 read_paper tool — `read_paper(path)` → `{sha256, page_count, bytes_len, path}`; opens fitz from the same bytes it hashes (`stream=data`) so the sha256 (future T15 cache key) and page_count can't disagree. Registered via import side-effect in `tools/__init__.py`. **Scope add (authorized):** resolved T06's carried-over input-validation item by inserting `jsonschema.validate` in `agent.py:_dispatch` before `fn(**input)` — bad/missing args now return a clean `is_error` instead of a raw `TypeError`; card's `input_schema` left verbatim (no `additionalProperties`, so extra args still TypeError → contained by existing except). Fixture `sample.pdf` copied from `papers/`. 2 offline tests green; `test_max_turns` regression green; 358 LOC total (2026-05-26)
- ✓ T08 end-to-end smoke — `src/palimpsest/__main__.py` CLI wires AnthropicProvider + CostMeter + Agent + TOOLS and prints `agent.run(argv[1])`; passes `{name: fn.tool_schema}` (Agent advertises schemas, dispatches by name). First real vertical slice: agent calls a tool, reads the paper, answers. **Scope add (authorized, card line 46 + user):** added `read_first_page_text(path)` (fitz first-page text) so the title actually surfaces — metadata-only `read_paper` made the model honestly refuse; logged in DEVIATIONS.md. CLI returns the real title ("Iridium single atoms… acidic conditions", Nat. Commun. 2022); spend €0.0161 on the ledger. `tests/test_e2e.py` (slow, key-gated, asserts title token + positive spend) + offline `tests/test_read_first_page_text.py`. Independent reviewer: no blockers, both findings applied. Offline suite 13 passed / 1 deselected (2026-05-26)

- ✓ T09 runpod bootstrap — manual infra task (no src). Account + credit + `RUNPOD_API_KEY` in gitignored `.env`. Started a pod via web UI and confirmed **SSH-over-TCP** works (`ssh root@… -i ~/.ssh/id_ed25519` → `root@…:~#`, Ubuntu 22.04). Used **RTX 3090** Community Cloud (4090 was "Low"; card allows any small GPU). Pod terminated, <$0.05. Gotcha: SSH key must be in RunPod Settings *before* deploy — restart doesn't re-inject it; mismatched key → silent fall-through to a (nonexistent) root password. `notes/runpod-bootstrap.md` records the **process only**, no real pod ID/IP/SSH/keys (per user; `notes/` gitignored). Verification: snippet 1 `runpod key ok`; snippet 2 → API status **200**, 0 pods (confirms nothing left running). Card's snippet 2 has a latent bug (`r.json().get("data")` but API returns a bare list) — auth assert passes first; logged in DEVIATIONS.md (2026-05-26)

- ✓ T10 dockerfile: base + docling — GPU image for the cloud parsers (`docker/palimpsest-gpu.Dockerfile` + `build.sh` + `README.md`). CUDA 12.8.1 + Py3.11 venv + `vllm==0.19.1` (cu128) + `docling==2.95.0`/`docling-ibm-models==3.13.2`, with `granite-docling-258M` baked (default + `untied` revisions). Build via **GitHub Actions → Docker Hub** (`.github/workflows/build-gpu-image.yml`, 4th file beyond the card — no local GPU/Docker), then a RunPod Custom Template. **Verified** on a RunPod RTX 3090 pod: `docling --version` → **Docling 2.95.0** (Core 2.77.1, IBM Models 3.13.2, Parse 5.11.0; Python 3.11.15, Linux x86_64). Four deviations, all logged in DEVIATIONS.md — none in extraction logic, all host/build-side: (1) card's CUDA 12.1 base stale → bumped, then (2) over-shot to 12.9 and corrected to **12.8** after measuring RunPod's host-driver ceiling (`nvidia-container-cli: cuda>=12.9` unsatisfied); 12.8 is also vLLM's floor + olmOCR's official image version, so all four parsers share it; (3) buildx provenance attestation made an OCI index RunPod rejected (`layers from manifest don't match image configuration`) → `provenance: false`; (4) `CMD ["bash"]` exited on detached start (`Container is not running`) → `CMD ["sleep","infinity"]`. Independent reviewer caught the torch/CUDA `--extra-index-url` resolution risk → added a build-time `assert '+cu128'`. Lesson: a GPU image's runnability is gated by the host (registry format, container liveness, driver CUDA), not by a green build (2026-05-27)

- ✓ T11 dockerfile: mineru + four-image pivot — Pivoted the GPU parser design from one stacked image to **four isolated per-image builds** (the four VLMs' torch/vLLM/transformers pins conflict; a shared env risks silently degrading a parser and corrupting the comparison). New standalone `docker/palimpsest-mineru.Dockerfile` (CUDA 12.8.1 + Py3.11 venv, `mineru[all]`, `opendatalab/MinerU2.5-2509-1.2B` baked); renamed `palimpsest-gpu.Dockerfile`→`palimpsest-docling.Dockerfile` (header only, build steps unchanged); `build.sh` parametrized per parser (`./build.sh <parser> [push]`); CI builds a matrix tagging `palimpsest/<parser>:0.1.0`. We build docling/MinerU/Chandra; olmOCR uses Allen AI's upstream image (hybrid sourcing, T12). Seeds torch from the cu128 index first and tightens the assert from `'+cu'` to `'+cu128'` so a wheel above RunPod's 12.8 host ceiling fails in CI, not on a paid pod. Also: bill GPU at the pod's **actual** RunPod rate (`adjustedCostPerHr`/`costPerHr`), not a hardcoded $0.34/hr. Cards T11–T16 + `palimpsest-v2-design.md` reconciled to the four-image, batch-by-parser model (one pod per parser, run the corpus, tear down). 2 rounds of independent review before commit (fixed a `parse_with_cache` partial-cache bug, reconciled the RunPodSession/parse_with_cache contracts across T14/T16 + the design doc, replaced a fabricated cache API with T15's real surface). **Pod-verified (per user, 2026-05-29):** image built and ran on a RunPod pod — `mineru --version` + baked weights confirmed working (specific output not captured in this log) (2026-05-28, 65612ea/ecef248/257bec2)
- ⏭ T12 olmOCR upstream image → RunPod template (superseded by T17 — olmOCR dropped, replaced with dots.ocr + PaddleOCR) — Registered Allen AI's upstream olmOCR image as a RunPod custom template (the four-image "hybrid" leg — olmOCR's pins are the tightest, so we use the upstream image as-is, **no Dockerfile**). Created `docker/runpod-template.md` with docling + mineru + olmocr entries (chandra → T13). Confirmed from the olmOCR README + root Dockerfile: image is CUDA 12.8 (`FROM vllm/vllm-openai:v0.11.2`) = RunPod's host-driver ceiling (no blocker); base `:latest` bakes no weights, `:latest-with-model` (~30 GB) bakes the **FP8** default model `allenai/olmOCR-2-7B-1025-FP8`. Chose `:latest-with-model` (FP8, olmOCR's own default config) per user — requires an **RTX 4090 (Ada)**, not the 3090 used in T09/T10 (Ampere has no native FP8). **Partially exercised on a pod (2026-05-28):** empirically corrected the RunPod Container Start Command to bare `sleep infinity` — `-c "sleep infinity"` exited the container (`container … is not running`) because RunPod shell-wraps the field, bypassing the image's `/bin/bash` entrypoint; confirmed proxy SSH (`ssh …@ssh.runpod.io`) reaches the custom image (no sshd in it); fixed the template. Also: any Ada GPU works for FP8 (RTX 4000 Ada 20 GB is the cheapest), not just the 4090. **Still not verified:** `python -m olmocr.pipeline --help`, the baked weights path (expect `…-1025-FP8`, not the card's `…-1025`), and FP8 init on a GPU — postponed, no GPU available when tried. Flip to ✓ after a real pod run (2026-05-28)
- ✓ T13 chandra image + all-four templates — Fourth/last cloud parser. New standalone `docker/palimpsest-chandra.Dockerfile` (CUDA 12.8.1 + Py3.11 venv, near-clone of the mineru image): the HuggingFace backend `chandra-ocr[hf]` (torch+transformers, run in-process), `datalab-to/chandra-ocr-2` (~5B, BF16, ~10 GB) baked via `snapshot_download`, `CMD ["sleep","infinity"]`. Added `chandra` to the CI matrix + header comment; `build.sh` already parser-parametrized (confirmed, no edit). Extended `docker/runpod-template.md` to all four (chandra table row + section: RTX 4090/3090, ~35 GB disk, `--help`/`pip show` verify, run `--method hf`). **Corrected 3 stale card facts from live sources** (PyPI/HF/DeepWiki, logged in DEVIATIONS): weights repo is `datalab-to/chandra-ocr-2` not `…/chandra` (the v2 default `MODEL_CHECKPOINT`; baking the old one would re-download on the pod); **no `--version` flag** → `chandra --help` + `pip show chandra-ocr`; ~5B not 4B (BF16 ⇒ Ampere 3090 OK, unlike olmOCR's FP8). **First CI build (d0d634b) FAILED** — `import vllm` → ModuleNotFoundError disproved the card's "Chandra brings its own torch/vLLM": base `chandra-ocr` is only the OpenAI-API client and bakes no runtime. Per user, switched to the HuggingFace backend (above); the cu128 assert is now `import torch, torchvision, transformers` (all GPU-free; torch+torchvision both `+cu128`-checked after independent review flagged torchvision could resolve off-index; DEVIATIONS has the full story). **Pod-verified (2026-05-29):** corrected build went green in CI, pulled + ran on a RunPod pod. `cuda avail: True`; `torch 2.11.0+cu128` + `torchvision 0.26.0+cu128` (the review's torchvision guard held on real HW) + `transformers 5.9.0`; `chandra --help` works (`--method [hf|vllm]`); baked weights complete at 9.9 G (`config.json` + single `model.safetensors`). GPU was an RTX PRO 6000 Blackwell (any BF16 GPU works; template recommends the cheaper 3090/4090). Live PDF extraction deferred to T16's per-parser code by design — not a gap (same ✓ bar as T10's `docling --version`) (2026-05-29, d0d634b/f5e3d22)

- ✓ T14 gpu_provider + idle watchdog — `RunPodSession` context manager (`src/palimpsest/parsers/gpu_provider.py`): `POST /pods` from a template → poll to `RUNNING`+`publicIp`+`portMappings["22"]` → read the pod's **actual** rate (`adjustedCostPerHr or costPerHr`, refuse if absent) → fabric direct-TCP SSH (`in_stream=False`; host key `AutoAddPolicy` since pods rotate keys; 10×3s connect-retry as RUNNING≠sshd-ready) → idle watchdog (daemon, soft 60s/hard 300s, `pending_work` flag) → teardown + bill wall-seconds → EUR via `cost_meter.record_gpu`. **Billing is computed at teardown (any thread) but written by `_flush_bill` on the entering thread only** — CostMeter's SQLite is thread-affine, so the watchdog must never call `record_gpu` (independent review caught this; a `try/finally` also guarantees the bill survives a failing `POST /stop`). Beyond the card (3→5 files, user-approved): `tests/conftest.py` (`--live` flag), `fabric>=3` in pixi.toml; `gpu_type` accepts `str|list[str]` so RunPod allocates an in-stock GPU (availability is dynamic). **Pod-verified (2026-05-29):** offline suite green (4 gpu tests incl. cross-thread + stop-failure billing regressions); live `test_real_pod_short` PASSED on a real **H100** pod (cheap GPUs were all capacity-blocked that night) — create→SSH→`echo hello`→ledger entry→teardown, pods cleaned to 0, ≈$0.26 spend. The first live run surfaced+fixed the fabric stdin-forwarding bug. **T16 inputs (logged):** parser images have no sshd (direct SSH can't reach them) and `scp_up/scp_down` are unproven — both are T16 decisions (2026-05-29). **T14 hardening from T17 pod-verify (2026-05-31, commits `fb44ef1` + `c753cac`):** iterate `gpuTypeIds` per id (multi-id rejected with HTTP 500; two distinct capacity strings matched as "no capacity"); `_await_running` default 60→180 tries (300s→900s) for SECURE first-pull; httpx Client timeout 30s→120s to stop the ReadTimeout-then-leaked-pod failure mode.
- ✓ T15 ParserCache + parser_runs DDL — `src/palimpsest/cache.py`: SQLite tables `papers(sha256 PK, filename, added_at, page_count, doi)` + `parser_runs(paper_sha256, parser_name CHECK IN ('docling','mineru','chandra','dots','paddle'), parser_ver, parsed_at, output_path, gpu_seconds, gpu_cost_eur, run_id, PRIMARY KEY (paper_sha256, parser_name, parser_ver))` — **5-parser set per the T17 swap** (the T15 card's original `olmocr` is gone). `ParserCache` class exposes `add_paper`, `has_all_parsers`, `get_output`, `insert_parser_run`, `list_unseen`. 9 offline tests in `tests/test_cache.py` green. Cache layout `cache/<sha>/<parser>.<ext>` keyed by SHA-256 of raw PDF bytes (T07 contract); `output_path` column stores a relative path. Stdlib sqlite3 only. (2026-05-30, 9fa4ce4 + 7c67b42)

- ✓ T17 drop olmOCR → 5-parser set (+ dots.ocr + PaddleOCR), sshd in all images, scp verify — Dropped **olmOCR** (FP8/Ada-only vendor image, no Dockerfile/sshd we control) and **expanded the comparison 4→5 parsers**: added **dots.ocr** (`rednote-hilab/dots.ocr`, ~1.7B layout VLM, MIT, transformers in-process/`sdpa`) + **PaddleOCR PP-StructureV3** (Apache, classic CV pipeline; the only **non-torch** image — CUDA **12.6**, since the cu128 Paddle index has no py3.11 wheel; runs on ≥12.8 hosts via backward-compat). Both emit bbox + structured tables → all 6 metrics apply to all 5. Parser-pick journey (per user, see DEVIATIONS): Nougat (dead/CC-BY-NC) → GOT-OCR2 (text-only, would blank 2 metrics) → **dots.ocr + PaddleOCR**. **sshd added to all five images** via shared `docker/start.sh` (injects each template's `$PUBLIC_KEY`, runs sshd, idles) so `RunPodSession`'s direct TCP SSH+scp reaches them; image tags bumped `0.1.0→0.2.0`; CI matrix now `[docling,mineru,chandra,paddle,dots]`. New files: `docker/{start.sh, palimpsest-dots.Dockerfile, dots_run.py, palimpsest-paddle.Dockerfile, paddle_run.py}`; sshd retrofit into the 3 existing Dockerfiles. Registry/docs/constitution swapped olmocr→dots+paddle (design doc `PARSERS`+SQL `CHECK`+F4 prose, CLAUDE.md, runpod-template.md, README, T12 marked superseded, T16). Added `test_scp_round_trip` (live). Methodology decided (user): per-parser scorecard now, parser-fusion deferred (data-gated). **CI build GREEN (2026-05-29) for all 5 images at `0.2.0`** — after fix `7f08b60` (`paddlepaddle-gpu` can't `import paddle` on the no-GPU CI builder; replaced with a `importlib.metadata` guard, and PP-StructureV3 weights now download on first pod run since the bake also imports paddle). **🟡 POD-VERIFY DEFERRED — no GPU available now; revisit when cheap GPUs are in stock** to run (a) ssh + a one-page parse per parser, (b) `scp_up`→`md5sum`→`scp_down` round-trip via `test_scp_round_trip`, (c) the `import paddle` + `is_compiled_with_cuda()` check we moved off CI, (d) finalize `dots_run.py`'s `sdpa`/JSON-parsing `[VALIDATE]` flags. (2026-05-29)
- ✓ T17 pod-verify pass 1 (partial, 3 PASS + 1 parse-ok-content-unread + 1 deferred) — Live pod runs on SECURE RTX 3090 (Community 3090/4090/A4000/A5000/L4 all out of stock at the time; SECURE 3090 ~$0.46/hr the cheapest available; A6000 fallback never needed). **Fully PASS (parse + retrievable content): docling, mineru, paddle.** Output sizes: docling 19 MB (645 text items + tables/pictures), mineru 315 KB (12-page list, equation_inline blocks + bbox, real paper title extracted), paddle 869 KB (`{markdown, pages}` envelope). **chandra parsed but content not retrieved**: pod exit-0 in 1705s, metadata records 33,809 tokens / 168 chunks / 4 images over 12 pages (so the model did the work) — but the verify script's "newest .json" picker grabbed `sample_metadata.json` (2.4 KB) not the `.md` content file; T16's `commands.py` must scp `<stem>/<stem>.md` explicitly. **dots ⏳** — `Qwen2_5_VLProcessor.check_argument_for_proper_class` refuses `video_processor=None` even with `transformers==4.55.0` pin at pod runtime; pinned to `4.51.3` in `palimpsest-dots.Dockerfile` for the 0.2.1 rebuild. Live spend €0.55 (one leaked RUNNING pod €0.075 from a httpx ReadTimeout pre-fix; chandra was the largest at €0.20 for 31 min on 3090 HF backend). Three T14 hotfixes committed mid-session: `fb44ef1` (iterate `gpuTypeIds` per-id — multi-id is NOT "any of"; RunPod 500s with two distinct strings), `c753cac` (broader 500-match + `_await_running` 300s→900s for SECURE first-pull + httpx Client timeout 30s→120s to stop ReadTimeouts leaking pods). One image-side fix landing here: `docker/dots_run.py` skips `videos=` kwarg when None (necessary but not sufficient for dots — still needs the transformers pin, applied here). **T17 followups for the 0.2.1 image bump:** (1) bake `libxcb1 libxext6 libgl1 libsm6 libglib2.0-0 libxrender1` into every Dockerfile (cv2 dlopen at parse time); (2) write `/etc/environment` in `start.sh` so sshd login shell inherits `/opt/venv/bin` on `PATH` without per-command prefix; (3) pin `vllm==0.19.1` in `palimpsest-mineru.Dockerfile` (`mineru[all]` does NOT pull it); (4) explicit output-file convention per parser for chandra/mineru picker (chandra: `<stem>/<stem>.md`; mineru: `<stem>/vlm/<stem>_content_list_v2.json`). `pixi run pytest` (full suite) → 27 passed / 2 live skipped, all green. (2026-05-31)
- ✓ T17 pod-verify pass 2 (0.2.1 bump, 4/5 fully PASS, dots still ⏳) — Five-image bump `0.2.0 → 0.2.1` (commit `8c5c962`) baked the runtime workarounds from pass 1: `docker/start.sh` writes `/etc/environment` so sshd inherits `/opt/venv/bin` on PATH; all 5 Dockerfiles gain `libxcb1 libxext6 libgl1 libsm6 libglib2.0-0 libxrender1` (cv2 dlopen); `palimpsest-mineru.Dockerfile` pins `vllm==0.19.1` explicitly. CI green for all 5; the 5 RunPod templates `PATCH /v1/templates/<id>` to `:0.2.1` in place via REST API (template IDs unchanged, no `.env` rotation). Re-verify on the new images: **chandra now FULL PASS** — `chandra … --method hf` writes `<stem>/<stem>.md` (84.5 KB on the 12-page sample) with the real paper title + authors + journal metadata + abstract opening; €0.18 / 1664s on SECURE 3090. Verify script no longer prefixes `export PATH=` or runs apt at boot — both baked, scp_up → parse → scp_down ran clean. **dots remains ⏳; root cause was deeper than a transformers pin**: dots.ocr's own `/opt/dots_ocr_src/requirements.txt` exact-pins `transformers==4.56.1`, which pip's resolver promotes over our `4.51.3` pre-install during `pip install -e /opt/dots_ocr_src` in the Dockerfile. Diag pod (sshing in, `pip show transformers`) confirmed `4.56.1` in the rebuilt image. dots.ocr's README documents `DotsMOCRParser(use_hf=True)` as the supported HF entry point; our `docker/dots_run.py` instead drives `transformers.AutoProcessor` directly, which trips the new `check_argument_for_proper_class` method on `video_processor=None`. **The fix is a code rewrite of `dots_run.py`** to use `DotsMOCRParser` (or equivalent dots.ocr-side loader) — defer to a focused dots session, not a Dockerfile tweak. Capacity note: a parallel dots-diag pod failed with `no instances currently available` because chandra had the only SECURE 3090 — when capacity is tight, parallel parser pods contend. Live spend this pass: chandra €0.18 + dots failed retries €0.005 + diag pod €0.001 ≈ **€0.19** (paddle/docling/mineru not re-verified — the 0.2.1 bake is a strict superset of the 0.2.0 runtime workarounds, no regression risk). `pixi run pytest` unchanged: 27 passed / 2 live skipped. (2026-05-31)
- ✓ T17 pod-verify pass 3 (dots FULL PASS, 5/5 verified; image rebuild pending) — Earlier pass-2 hypothesis was wrong: `DotsOCRParser(use_hf=True)` (note: real class is `DotsOCRParser`, README's `DotsMOCRParser` is a typo) is **not** the fix — its own `_load_hf_model` calls bare `AutoProcessor.from_pretrained(..., use_fast=True)` and hits the same `Qwen2_5_VLProcessor.check_argument_for_proper_class` guard. Probe (sshd into a dots pod, inspect `transformers.processing_utils`) confirmed `check_argument_for_proper_class` is now a `ProcessorMixin` method (no longer module-level), called from `Qwen2_5_VLProcessor.__init__` line 96, rejecting `video_processor=None`. `use_fast=True` silently falls back to the slow processor for dots.ocr's trust_remote_code weights (no fast variant published) so it doesn't avoid the guard. Real fix: **monkey-patch `ProcessorMixin.check_argument_for_proper_class`** to allow `arg is None` when `attribute_name == "video_processor"` — image-only models legitimately have no video processor. Patch landed in `docker/dots_run.py`. Verified on SECURE L4 (3090/A4000 SECURE both out at the time): `python /opt/dots_run.py …` → exit 0, **72,207 chars markdown over 12 pages, 165 KB envelope `{markdown, pages}`**, real paper title + authors + DOI extracted; €0.08 / 1040s. Verify used the `patch_file` mechanism (scp the local `docker/dots_run.py` over the baked `/opt/dots_run.py` on the 0.2.1 image) — proves the fix without an image rebuild. **For T16 to use the canonical image, a CI rebuild is still needed** (bake the dots_run.py fix into the dots image at `0.2.1` or `0.2.2`); deferred until the user pushes. With that rebuild + a `PATCH /v1/templates/<dots-id>` to the new tag, T17 closes ✓ for all 5 parsers. Live spend this pass €0.08 + 4 diag pods ≈ €0.005. (2026-05-31)
- ✓ T16 parse_with_cache + PARSERS registry (live verified) — `src/palimpsest/parsers/commands.py` is the `PARSERS` literal dict (5 entries: docling, mineru, chandra, dots, paddle), each with `template_id_env` + `run_cmd(inp, outdir)` + `pod_output(inp, outdir)` + `version`. **Deviates from card's "uniform `{parser}.json`" output**: T17 pod-verify exposed that real output paths differ per parser (chandra writes `.md`, mineru writes deep into `<stem>/vlm/`, docling writes `<stem>.json` in `--output` dir). `pod_output` returns the actual on-pod path; runner scps to `cache/<sha>/<parser><suffix>` where suffix is taken from the path (`.json` for 4, `.md` for chandra). Honest reflection > shell-mv gymnastics. `src/palimpsest/parsers/runner.py`: `parse_with_cache(pdf_paths, cost_meter, cache) -> dict[sha, dict[parser, Path]]`. Hashes via T07 `read_paper`; registers papers FIRST (parser_runs FK); prefills mapping from cache; short-circuits if all sha × all parsers cached; else loops parsers (outer) → unseen PDFs (inner) inside one RunPodSession per parser with `idle_hard=2400` + ssh `timeout=2400` (covers chandra worst case ~1700s on the 12-page sample; larger corpora need more). Per-(sha, parser) failure: log loudly, skip the insert (no poisoned row → future retry open), continue with next pair; all 5 pods still attempted regardless. `tests/test_runner.py` covers 4 cases via `_FakePodSession` mock: **(a)** happy path (5 pods, complete mapping), **(b)** all-cached short-circuit (`RunPodSession` NEVER instantiated, load-bearing assertion), **(c)** mixed/per-parser skip (one cached parser → its pod skipped, others run, mapping mixes cache + fresh), **(d) failure-path** (one parser raises mid-batch → cell omitted, no parser_runs row written, other 4 still succeed, batch doesn't crash). Full suite: 31 passed / 2 live skipped, no regressions. Independent advisor review caught the missing failure-path test pre-commit; added before final commit (no separate hotfix). **Live verified (2026-05-31, SECURE L4, €0.32 / ~1h wall):** first attempt parsed docling/mineru/chandra/dots cleanly (€0.28) then hit a transient HTTP 500 on the paddle pod-create — runner CRASHED instead of skipping. Real defect: my failure-handling try/except was INSIDE the `with RunPodSession(...) as pod:` block, around the per-PDF parse loop. `RunPodSession.__enter__` raises (pod create / `_await_running` / SSH handshake) escaped. Fix in `a70347e`: wrap the entire ctx-manager in try/except → log + advance to next parser on pod-level failure; per-PDF handling preserved inside. New test `test_one_parser_pod_create_failure_skips_parser_continues` covers the lifted layer; 5/5 mocked tests green. Re-run with 4 parsers already cached → only paddle pod spun (213s, €0.02). Final cache `cache/<sha>/`: `docling.json` 19 MB, `mineru.json` 307 KB, `chandra.md` 82 KB, `dots.json` 161 KB, `paddle.json` 846 KB. parser_runs has 5 rows. Per-parser wall times match T17 verify within noise. Total live spend €0.32 < card target €0.60. (2026-05-31, commits `a518606` + `27168d9` + `a70347e`)
- ✓ T17 closed — 0.2.2 ships, 5/5 verified on canonical images — `dc9ef5d` bumped tags 0.2.1 → 0.2.2 across `docker/build.sh`, `.github/workflows/build-gpu-image.yml`, `docker/runpod-template.md` (table + 5 per-parser headers); baked `docker/dots_run.py`'s `ProcessorMixin.check_argument_for_proper_class` monkey-patch into the dots image; restructured `runpod-template.md` with a tag-as-contract preamble + version history (0.1.0 → 0.2.0 sshd → 0.2.1 PATH+cv2+vllm → 0.2.2 dots-patch) + a baked-vs-caller-side split for T16. Also dropped the failed `transformers==4.51.3` pin from `palimpsest-dots.Dockerfile` (dots.ocr's `requirements.txt==4.56.1` always wins; pin was dead code) and replaced with a comment naming `dots_run.py` as the real fix site. CI green for all 5 at `0.2.2`; 5 RunPod templates `PATCH`ed to `:0.2.2` in place (template IDs unchanged; HTTP 200 each). **Re-verified dots on canonical `:0.2.2` (no `patch_file`):** SECURE L4, €0.06 / 820s, 165 KB envelope, 12 pages, 72,207 chars markdown — identical output to the pass-3 patched run, confirming the baked file matches the repo. **T16 unblocked** for all 5 canonical images via the existing 5 template IDs; no per-parser runtime workarounds remain in caller code. Total live spend across the three T17 verify passes ≈ €0.85 (vs €50 budget). (2026-05-31)
- ✓ T17 cache-hit verification test (separate body of work per the card's "naming note" — distinct from the parser-infra T17 lines above; shares the number for git history) — `tests/test_cache_hit.py` (~95 LOC, single test `test_second_call_uses_cache`). Calls `parse_with_cache([sample_pdf], meter, cache)` TWICE in a fresh `tmp_path`: first with a `_FakePodSession` mock that materializes outputs (cache populates from empty); second with a `_PoisonedPodSession` whose `__init__` AND `__enter__` BOTH raise `AssertionError("cache miss: …")`. Strictly stronger than the card's literal "mock `__enter__` to fail loudly" — also traps the "instantiated but never entered" failure mode, matching the card's added "NEVER instantiates RunPodSession" clause. Belt-and-suspenders: also asserts `meter.total_eur()` is unchanged between calls — but with the fake first-call pod that doesn't bill, this reduces to `0 == 0` (honest decoration; the sentinel is the load-bearing assertion, independent advisor caught this and approved as-is). Pattern mirrors `test_runner.py:test_all_cached_short_circuit` (T16 case b) but lifted into its own dedicated file because the cache short-circuit is the budget-critical invariant for the €50 cap and deserves a focused regression test. Verification: `pixi run pytest tests/test_cache_hit.py -v -s` → 1 passed, output includes "second call used cache". Full suite: 33 passed / 2 live skipped, zero regressions. No src changes; pure offline test. (2026-06-01)
- ✓ T48 parser hardware verify (document-only) — The card's 5-parser-on-real-GPU bar was **already met** by the T16/T17 live runs: 5 `parser_runs` rows with real `gpu_cost_eur` + 5 cached outputs for `sample.pdf` (`sha=3432d049…`, 12 pages) on canonical `0.2.2` images, SECURE RunPod GPUs (2026-05-31). The card's "never run on real GPU" premise is stale; its verify command `python -m palimpsest parse …` does not exist (no `parse` subcommand — `__main__.py` is the T08 agent loop; `parse_with_cache` is the real library surface). Per user (document-only, **no new GPU spend**), wrote `notes/parser-hardware-verify.md` (gitignored) consolidating the scattered PROGRESS/DEVIATIONS evidence into the card's designated file: per-parser PASS status, wall seconds, € cost, output size, and corrections. Authoritative numbers from `palimpsest.db parser_runs`: docling 32.3s/€0.0032/19MB, mineru 129.5s/€0.0129/314KB, paddle 213.3s/€0.0213/866KB, dots 788.0s/€0.0785/165KB, chandra 1055.8s/€0.1052/84KB — all 5 PASS. Per-parse attribution (`parser_runs`) sums €0.221; per-pod billed total (`cost_ledger`, 5 `gpu` pod_session rows, full pod wall incl. pull+idle) €0.305 < card's ~€0.4–0.9 (whole ledger €0.32 incl. €0.016 LLM dev-smoke from T04/T08, not parser cost; the ~€0.85 in PROGRESS spans 3 earlier T17 verify passes whose pods were not recorded to this DB). **No new corrections found** (all fixes — mineru vllm pin, dots `check_argument_for_proper_class` monkey-patch, chandra output-picker, paddle non-torch CUDA 12.6, sshd/PATH/cv2 host bakes, T14 gpuTypeIds/timeout hardening — were logged in DEVIATIONS.md during T16/T17), so DEVIATIONS.md left untouched. Verification (both pass against repo): `ls cache/3432d049…/` → 5 files; `sqlite3 … parser_runs` → 5 rows all non-zero. No src/schema/test/dep touched. (2026-06-11)

## Week 3 — schema & extraction

- ✓ T18 LinkML schema + EMMO ECHO resolver — `schema/palimpsest.yaml` (14 classes, 36 slots; EMMO + QUDT + PROV-O + schema.org + palimpsest-local) + `src/palimpsest/ontology.py` (`EMMO_ECHO` + `@cache emmo_iri()` reading `rdfs:label`, `KNOWN_IRIS` ×8 hashes verbatim from design Appendix E) + `tests/test_ontology.py` (1 verified-hash test + 8 hash-as-class parametrized + 8 label-resolves parametrized = 17 tests). **Probe-first per advisor:** fetched ECHO (4.6 MB → `~/.cache/palimpsest/echo.ttl`) BEFORE writing tests — confirmed all 8 KNOWN_IRIS CamelCase labels resolve under `rdfs:label` (ECHO uses CamelCase as both `rdfs:label` AND `skos:prefLabel`), AND resolved 6 more hashes the design left to schema-gen time (ExchangeCurrentDensity, ECSA/ElectrochemicallyActiveSurfaceArea, ElectrodePotential, Acidic/AlkalineElectrolyte, LinearPotentialRamp); pasted verbatim into YAML with `# Hash resolved via emmo_iri (2026-06-01)` comments — no placeholder CURIEs that would expand to bogus IRIs. Also probed `linkml_runtime.linkml_model.units.UnitOfMeasure` field shape (`ucum_code` + `exact_mappings`, NOT `iri`) BEFORE writing 14 `unit:` blocks — original plan had `iri: qudt:MilliV` which would have failed linkml-validate (advisor catch, 30s saved a rewrite). **Decoupled test families** (advisor's blind-spot fix): `test_known_hashes_still_declared_as_class` catches EMMO removing/renaming the hash (real breaking change); `test_known_labels_resolve` catches EMMO renaming the human label (cosmetic) — separated so a red-on-one / green-on-other tells you which mode failed. **Deviations from card / following Appendix E** (Appendix E is the IRI source of truth, so following it consistently throughout): TODO_EMMO_UPSTREAM on **6** entities (card line 22 names 4; Appendix E adds `overpotential_at_10mAcm2` line 651 and `electrode_potential_vs_rhe` line 663 — equally upstream-blockable); Condition class has **6** slots not 5 (added `scan_rate` per Appendix E line 664) and renamed `potential_vs_RHE` → `electrode_potential_vs_rhe` (Appendix E line 663); `bbox` has `minimum_cardinality: 4` + `maximum_cardinality: 4` (advisor's 2-line tuple-fidelity insurance vs Appendix E's loose `tuple[float×4]`). **Verification:** `pixi run linkml-validate schema/palimpsest.yaml` → "No issues found"; `pixi run pytest tests/test_ontology.py -v` → **17 passed in 1.63s** (warm-cache; cold-cache ~4s for ECHO fetch+parse); full suite **50 passed / 2 live skipped** / 4 warnings (3 new `PytestUnknownMarkWarning` for `@pytest.mark.slow` matching pre-existing `test_e2e.py:26` pattern — not my mess per CLAUDE.md §3). No new deps (linkml + linkml-runtime + rdflib + httpx all pre-pinned in pixi.toml). (2026-06-01)

- ✓ T19 LinkML schema generation pipeline — `pixi.toml [tasks]` gains 5 entries per card: `schema-{pydantic,shacl,jsonld,jsonschema}` (each `gen-* schema/palimpsest.yaml > schema/generated/<file>`) + aggregate `schema = depends-on [the 4]`. 4 artifacts generated + committed to `schema/generated/` (pydantic.py 19k/321 LOC, shacl.ttl 15k, context.jsonld 5.3k, jsonschema.json 18k) so clones need zero codegen. **Headers:** `# GENERATED — DO NOT EDIT BY HAND. Run: pixi run schema` for `.py`/`.ttl`; top-level `"_generated"` key for `.json`/`.jsonld` (JSON has no comments — user choice). **Hand-added, NOT pipeline-injected** per advisor pushback on overengineering (a helper script vs 4 `Edit` calls is not "minimum code"); regen wipes them, four header-canary tests fail loudly so the wipe is never silent. **Card deviation:** card example writes `Overpotential(value=236.0, unit="qudt:MilliV", ...)` but real slot is `unit_label` (Measurement, free-text per Appendix E); test uses `unit_label="mV"` and asserts BOTH slots so a future rename catches (advisor: a one-slot test trivially passes since every field is `Optional`). `schema/generated/` works as a **PEP 420 namespace package** (no `__init__.py`; verified via `python -c` probe before writing the test). **Post-merge audit (Read all 4 artifacts end-to-end) → schema-layer gaps, NOT generator bugs.** **Scope add (authorized, user-requested):** patched T18 schema — `Evidence.{paper, page, bbox, parser_name}` AND `Paper.sha256` → `required: true` per CLAUDE.md provenance non-negotiable (raw audit found all-Optional Evidence; advisor's reconciliation catch flagged the second hole — making `Evidence.paper` required without `Paper.sha256` required leaves the "paper_hash" half of the triple identity-less; T22's LLM could have shipped Evidence(paper=Paper(), …) and validation would have accepted it). Regen + re-headers + 2 new tests: `test_evidence_requires_provenance_fields` (empty fails, missing-one fails, full succeeds + source_text stays None) and `test_paper_requires_sha256` (Paper() fails, doi/title/authors stay optional). Two non-blocking findings (empty `OxygenEvolutionReaction` marker class, 5 orphan JSON-LD slots) tracked in `tasks/T18a-schema-cleanups.md`. `tests/test_schema_gen.py` final: 9 tests (3 card + 4 header canaries + 2 required-field). **Verification:** `pixi run schema` regenerates clean; `pixi run pytest tests/test_schema_gen.py -v` → **9 passed in 0.12s**; full suite **59 passed / 2 live skipped**, no regressions (was 50p/2s after T18; +9 from new file). The two verification commands run at SEPARATE points (regen wipes headers; chaining fails header tests by design). No new deps (linkml pre-pinned). (2026-06-01)

- ✓ T20 oer-extraction SKILL.md — `skills/oer-extraction/SKILL.md` (frontmatter verbatim from card: `name`, `description`, `when_to_use`, `version=1.0.0`) + body **5831 chars (~1500 tokens, well under card's ~3000-token cap)** covering all 7 card bullets in card order: required slots/conditions (η requires `j`, mass activity requires `E vs RHE`, TOF requires `E` + site-counting method), RDE vs PEMWE η defaults (10/100 mA cm⁻² vs 1/2 A cm⁻²), Tafel conventions (mV/dec + fit range), mass activity (A g⁻¹Ir + potential), stability (h + cell type + degradation rate), mechanism flags (AEM/LOM/WNA + evidence type), common traps (iR-correction tri-state, scan rate, geom vs ECSA-norm, electrolyte, loading). Two reference files: `references/tafel-conventions.md` (sign convention, intercept handling, fit range as the most-omitted field) + `references/pemwe-protocols.md` (75–80 °C, DI water, Nafion 117/115/212, anode loading 0.1–2 mg cm⁻² Ir, polarization + durability + AST protocols). **Output discipline:** SKILL.md closes with a section reminding the LLM to ship one `Measurement` per (variable, conditions) tuple and that Evidence must carry `paper.sha256`+`page`+`bbox`+`parser_name` per T18's provenance non-negotiable. **Verification:** card's `pixi run python -c "..."` snippet PASS — frontmatter parses, `name=='oer-extraction'`, `version=='1.0.0'`, `len(body)==5831 > 1000`. No src/schema/test/dep touched. **Scope add (authorized, surfaced by user during review):** drafted `tasks/T20.5-normalize.md` to design **central** `src/palimpsest/normalize.py` (UNIVERSAL_UNITS + UNIVERSAL_ENUMS: `iR_correction`, `normalization_basis`, `cell_type_family`, `electrolyte_family`, `scan_rate_regime` + loader + `build_normalization_prompt` merger) **with per-skill `normalization.yaml` overlays** — addresses per-skill duplication of universal enums/units that would have hit every future SKILL.md (HER, CO2RR, NRR). SKILL.md prose stays the LLM narrative; YAML overlay becomes the machine-readable layer. T22 calls `build_normalization_prompt` to inject merged universal + active-skill rules into the system prompt; conflict policy raises if a domain overlay tries to redefine a universal enum. T20.5 ships separately as src work. (2026-06-01)

- ✓ T20.5 central normalize.py + per-skill normalization.yaml overlay — `src/palimpsest/normalize.py` (107 LOC) ships **UNIVERSAL_UNITS** (12 entries after the post-review fix: 7 Measurement subclasses `Overpotential`/`TafelSlope`/`ExchangeCurrentDensity`/`ChargeTransferCoefficient`/`MassActivity`/`TurnoverFrequency`/`ECSA` + 5 Condition slots `current_density`/`temperature_C`/`scan_rate`/`electrolyte_ph`/`electrode_potential_vs_rhe` — keyed by actual schema entities; see post-review fix below) + **UNIVERSAL_ENUMS** (5 enums: `iR_correction`, `normalization_basis`, `cell_type_family`, `electrolyte_family`, `scan_rate_regime`) + `load_skill_normalization(skill_dir) -> dict` (missing file → `{}`, empty file → `{}` via `or {}` guard; malformed YAML lets `YAMLError` bubble as a programmer error) + `build_normalization_prompt(active_skill_dirs) -> str` (merges universal + each active overlay into one markdown block; `yaml.safe_dump`s the domain payload verbatim so new skills add keys without touching the renderer). `skills/oer-extraction/normalization.yaml` (45 lines) overlays the OER-specific buckets: 4 `operating_points` (RDE_10mA / RDE_100mA / PEMWE_1A / PEMWE_2A, each with current_density + cell_type_family), `mechanisms` (AEM/LOM/WNA/mixed/unspecified + 5 evidence_types), 9 `active_metals`, 4 `electrolytes` (H2SO4/HClO4 at 0.1/0.5/1 M), 6 `pemwe_membranes`. **Advisor-widened from card-literal** (card said "enum collision raises"; advisor caught that a domain overlay redefining a UNIVERSAL_UNITS key would also silently contradict the universal "Canonical units" section above): conflict check is now `set(overlay) & (set(UNIVERSAL_ENUMS) | set(UNIVERSAL_UNITS))`, user-approved. Error message uses `Path(skill_dir).name` not the YAML's claimed `domain:` field so a misconfigured overlay can't mislabel its own error. **Verification:** `pixi run pytest tests/test_normalize.py -v` → **6 passed in 0.06s** (5 card + 1 from parametrize-split: `test_universal_constants_well_formed`, `test_load_skill_normalization_oer`, `test_load_skill_normalization_missing_file`, `test_build_normalization_prompt_contains_both_layers`, `test_overlay_conflict_raises[enum]`, `test_overlay_conflict_raises[unit]`); card's `python -c` snippet PASS (2091-char merged block contains `iR_correction` + `RDE_10mA` + `LOM`); full suite **65 passed / 2 live skipped** (was 59/2 after T19, +6 from new file, 0 regressions). No schema or agent.py change — T22 wires `build_normalization_prompt` into the system prompt; T18a still owns schema-level LinkML enums. SKILL.md prose unchanged (T20 sealed); machine-readable layer is the YAML sibling. (2026-06-01)

- ✓ T20+T20.5 schema-alignment fix (post-review) — Independent `reviewer` subagent caught drift between T20/T20.5 contents and the actual T18 schema entities. **Blocker:** `UNIVERSAL_UNITS` in `normalize.py` and the SKILL.md "Required slots" table referenced non-existent slot names (`overpotential`, `stability_hours`, `pemwe_cell_voltage`, `pressure`) and wrong units (`ecsa: m2/g` but `schema/palimpsest.yaml:249` declares `cm2`; `temperature` but schema slot is `temperature_C`). LLM following the original prompt would have emitted data the LinkML schema can't validate. **Fix:** rewrote `UNIVERSAL_UNITS` to enumerate real entities only — 7 `Measurement` subclasses (`Overpotential`, `TafelSlope`, `ExchangeCurrentDensity`, `ChargeTransferCoefficient`, `MassActivity`, `TurnoverFrequency`, `ECSA`) + 5 `Condition` slots (`current_density`, `temperature_C`, `scan_rate`, `electrolyte_ph`, `electrode_potential_vs_rhe`) with their schema-declared UCUM codes (`cm2`, `Cel`, `mV/s`, `[pH]`, `V`). Rewrote SKILL.md required-slots table as **"Reported variable | Schema target | Required conditions"** with concrete class names per row; `Stability` and `PEMWECellVoltage` marked as **needing T18a F3** (no schema class yet, record as free-text annotation until added). **Contract drift in own files (between T20 prose and T20.5 UNIVERSAL_ENUMS):** SKILL.md said `iR_correction = false` (wrong; enum is `applied/not_applied/unknown`) → fixed to `not_applied`; `references/tafel-conventions.md` said `(true / false / unknown)` → fixed; PEMWE operating points labeled "η@1 A cm⁻²" but those are Vcell, not η → fixed. **T18a card:** added Finding F3 tracking missing Measurement subclasses (`Stability`, `PEMWECellVoltage`, optional `SpecificECSA` for m²/g, optional `Pressure`) and the corresponding `UNIVERSAL_UNITS` additions to make in the same change. Unit-collision test updated to use `temperature_C` as the colliding key (was `tafel_slope`; after the rewrite the class name is `TafelSlope`). SKILL.md body grew 5831 → 6651 chars (still ~1700 tokens, well under card's 3000 cap). **Verification:** card's `python -c` snippet PASS; 6/6 normalize tests green; full suite **65 passed / 2 live skipped** (zero regressions). 5 files touched: `src/palimpsest/normalize.py`, `skills/oer-extraction/SKILL.md`, `skills/oer-extraction/references/tafel-conventions.md`, `tasks/T18a-schema-cleanups.md`, `tests/test_normalize.py`. Non-blocking nits (YAML fence, type-check guard, multi-skill test, McCrory citation precision, `active_metals` acid-stability split) deferred per agreed scope. (2026-06-01)

- ✓ T21 SkillLoader + read_skill tool (progressive disclosure) — `src/palimpsest/skills.py` (73 LOC): `SkillLoader` scans `skills/*/SKILL.md` once at `__init__`, parses **only** YAML frontmatter (per-skill cost in the cached system prompt stays ~50–100 tok); body is read lazily by `load(name)`. `manifest() -> str` returns one line per skill formatted `- {name}: {description}` for T22's system-prompt injection; `load(name) -> str` returns the body `.strip()`-ed (deterministic for the `>1000` assertion across trailing-newline edits); unknown name raises `KeyError(name)`. `_split(text)` slices on the first two `---` fences via `splitlines(keepends=True)` + `rstrip() == "---"` (handles `--- ` trailing-whitespace fences); raises `ValueError` loudly on missing/unclosed fence per CLAUDE.md §1. `src/palimpsest/tools/read_skill.py` (32 LOC): `@register("read_skill", {...})` with the card's schema literal byte-exact; module-level `_LOADER = SkillLoader()` (one process-wide scan, `Path("skills")` resolves vs CWD which is the project's repo-root convention); `KeyError` caught → friendly string `"unknown skill: 'foo'. Available: oer-extraction"` so the agent's `_dispatch` returns it as a non-error tool result (LLM self-corrects, loop doesn't crash). `tools/__init__.py` +1 import line mirroring T07's `read_paper` side-effect pattern. **PyYAML not a direct pin** in `pixi.toml` — arrives transitively via `linkml`, precedented by `normalize.py:20`; no new dep. **Independent `reviewer` subagent verdict: PASS_WITH_CAVEATS, 0 blockers, 10 non-blocking.** Applied 2 style-match edits per CLAUDE.md §3 (relative `from . import register`, single-line `@register("name", {` opener — matching `read_paper.py:15,18`). **Deferred to T22** (per reviewer's "expand beyond one skill in T22" framing): duplicate-name `ValueError` guard, missing-`name`/`description` `meta.get()` fallback — both speculative until a 2nd skill exists. **Deferred indefinitely (speculative per CLAUDE.md §2):** CRLF/BOM normalization in `_split` (no such files exist), `_LOADER._skills` private-attr smell (one caller, refactor cost > smell). **Verification:** `pixi run pytest tests/test_skills.py -v` → 3 passed (2 card + 1 KeyError-contract test that the tool wrapper's catch depends on); full suite **68 passed / 2 live skipped** (was 65/2 after T20.5, +3 from this card, zero regressions). Sanity probe: `TOOLS` = `['read_first_page_text', 'read_paper', 'read_skill']`; manifest is one line; `load("oer-extraction")` returns 6648 chars (matches T20's 6651 minus stripped trailing whitespace); `read_skill("nope")` returns the clean fallback string. Does NOT touch `agent.py`, `__main__.py` (T22 wires manifest into system prompt), or `skills/oer-extraction/SKILL.md` (T20 sealed). (2026-06-01, `ab661d4`)

- ✓ T22 extract tool (parser output → Sonnet → Pydantic) — `src/palimpsest/tools/extract.py` (283 LOC: introspect-built `_CLASS_MAP` from `schema/generated/pydantic` excluding the abstract `Measurement` base + LinkML support classes + **pydantic re-exports** `BaseModel`/`RootModel` filtered via `obj.__module__ == _schema.__name__` (independent reviewer caught: `inspect.getmembers` would surface them and `BaseModel(**item)` raises `PydanticUserError`, killing the batch); cached system prompt = role + skill body + normalization rules + full JSON schema + output contract ≈ 7K tokens; `cache_breakpoints=["system"]` per card preferred). **Signature widened from card** `list[BaseModel]` → `tuple[list[BaseModel], list[(Exception, dict)]]` per card's "Notes" bullet so T23/T24 can re-prompt on failures (resolved with user pre-implementation; logged in `~/.claude/plans/`). Optional `cost_meter` / `provider` / `cache` kwargs (test injection + budget tracking without touching `cost.py`/`provider.py`/`cache.py`). **Two import cycles to defer** (`palimpsest.cache` + `palimpsest.agent` lazy inside `extract()` body — `cache.py:18` imports `tools/read_paper`, so any tool importing cache.py back creates the cycle). **Two provenance gates, applied at the earliest possible layer per CLAUDE.md non-negotiable:** (1) `_inject_provenance` overwrites the **caller-owned** fields `paper.sha256` + `parser_name` BEFORE `_instantiate` so an LLM-hallucinated sha can never reach the graph (advisor catch pre-live); (2) `extract()` loop **routes Measurement subclasses with no `evidence` dict to `errors`** rather than letting them silently pass as `valid` with `evidence=None` (reviewer catch — original code punted to T23/SHACL, which would have wasted T24's insertion cost on items the agent could have re-prompted cheaply). System prompt updated to explicitly say "do NOT emit `paper.sha256` or `parser_name`" and "do NOT emit `type: Measurement` (abstract)"; `type` list shown to LLM now scoped to the 7 Measurement subclasses (not the full `_CLASS_MAP`). **9 offline tests + 1 live test** (`tests/test_extract.py` 377 LOC, mirrors `test_agent.py`'s `_StubProvider`+`load_dotenv` pattern): happy path / validation error partial (uses `notes` extra field — `ConfiguredBaseModel.extra="forbid"` fires the error) / unknown class / partial evidence fails (missing `page` → ValidationError per T19 audit) / **provenance sha overwritten** (advisor regression — stub emits "WRONG_HALLUCINATED_SHA", asserts valid instance has real sha) / **baseclass type does not crash batch** (reviewer regression — stub emits `type: BaseModel`, asserts batch survives, item in errors) / **missing evidence rejected** (reviewer regression — item without `evidence` key routes to errors, doesn't silently pass) / **items not a list raises ValueError** (reviewer regression — `{"items": {...}}` doesn't crash with AttributeError) / cache miss raises FileNotFoundError. **Live verify on T16-cached sample paper (2026-06-02, pre-reviewer-fixes):** 12 valid + 5 errors (all `KeyError("Measurement")` — model emitting abstract base, correctly filtered, now also nudged against by the post-review prompt change), spend **€0.2785** (target €0.30, sub-€1 sanity cap held). Items: 4 Overpotential @10mA/cm² (236/298/412/511 mV — textbook IrO₂ benchmark from the cached PEM paper), 4 TafelSlope (52.6/75.8/109.8/131.3 mV/dec), 2 MassActivity (3343/65 A g⁻¹_Ir), 2 TurnoverFrequency (1.665/0.0237 s⁻¹). Card preferred 1-hour cache TTL; accepted default 5-min ephemeral since `provider.py` is sealed (user choice) — **revisit if batch hit rate suffers**. **NOT touched per card:** `agent.py`, `cost.py`, `cache.py` (and `provider.py` by extension). Two rounds of independent review (advisor pre-live caught hallucinated-sha; `reviewer` subagent pre-commit caught BaseModel-leak + silent missing-evidence) before final commit. 77 passed / 3 live skipped (was 68/3 after T21; +9 new tests, 0 regressions). (2026-06-02)

- ✓ T22 post-review polish — Five reviewer non-blockers + one missing test addressed in a second pass after `3a814af`. **(NB #6)** `raw = dict(item)` → `raw = copy.deepcopy(item)` in `extract.py`'s error-routing loop; safe today but removes fragility if a future fix mutates nested `evidence` keys. **(NB #9 + NB #2)** Dual `SkillLoader()` scan deduped — `extract.py` drops its own `_LOADER = SkillLoader()` and imports `read_skill._LOADER`; sibling tools now share one process-wide scan. Also added `SkillLoader.names() -> list[str]` to `skills.py` (2 LOC) so both `read_skill.py` and `extract.py` can drop the `_LOADER._skills` private-attr access in their friendly-error fallbacks. **(NB #10)** `_load_skill_dir` one-line wrapper inlined as `Path("skills") / skill_name` at its single call site. **(NB #1, second-pass reviewer catch)** Misleading import comment rewritten — original claim "_read_skill is imported above extract in __init__.py_" was empirically false (Python's import machinery loads `read_skill` on demand from `extract.py`'s `from .read_skill import _LOADER`, no ordering dependency). **(NB #5)** Drift-prevention comment added to `read_skill.py:14-17` warning future maintainers not to construct another `SkillLoader()` elsewhere. **(Missing test #4)** `extract()` now wraps `_LOADER.load(skill_name)` with `try/except KeyError → raise ValueError(f"unknown skill: {skill_name!r}. Available: {avail}") from None` so the agent's `_dispatch` surfaces a re-promptable message rather than the bare `'nope'` from `str(KeyError("nope"))`; new test `test_unknown_skill_name_friendly_error` pins the consumer-facing format. SETUP.md doc-pointer bumped T22 → T23 (T22 fully done). 6 files touched: `src/palimpsest/skills.py` (+5 LOC), `src/palimpsest/tools/read_skill.py` (+2 LOC), `src/palimpsest/tools/extract.py` (net +5 LOC, 292 total), `tests/test_extract.py` (+22 LOC, 400 total), `PROGRESS.md`, `SETUP.md`. Second independent `reviewer` subagent verdict: **PASS_WITH_CAVEATS, 0 blockers, 5 NBs all addressed** in this pass. **NOT touched still per card:** `agent.py`, `cost.py`, `cache.py`, `provider.py`. Verification: `pixi run pytest tests/test_extract.py -v` → 10 passed / 1 skipped; `pixi run pytest` → **78 passed / 3 live skipped** (was 77/3 at `3a814af`; +1 new test, 0 regressions). Did NOT re-run live extract — reviewer flagged this as optional, the prior live run is still valid since the post-fix changes are pure refactoring + a friendly-error wrap that doesn't affect the LLM call path. (2026-06-07)

- ✓ T23 SHACL validation step — `src/palimpsest/validation.py` (87 LOC) replaces the empty stub: `validate_instance(BaseModel) -> (bool, str)` and `validate_batch(list[BaseModel]) -> list[(BaseModel, bool, str)]` per card. Pipeline: `model_dump(mode='json', exclude_none=True)` → `_walk_attach_types` injects `@type` at root + each nested `BaseModel` field (the schema has no `list[BaseModel]`; walker docstring acknowledges) → `{"@context": ctx, **data}` → `rdflib.Graph().parse(format='json-ld')` → `pyshacl.validate(... shacl_graph=_shapes(), inference='none')`. `_shapes()` and `_context()` are `@lru_cache(maxsize=1)` so the TTL+JSON parses happen once per process. No new deps (`rdflib` + `pyshacl` already pinned). **Card pivot (user-approved via AskUserQuestion pre-implementation):** the card's literal "Overpotential missing condition → SHACL fails" example is wrong against the currently generated `shacl.ttl` — LinkML emits `sh:minCount` only for slots marked `required: true`, and `Measurement.condition` isn't. Only 5 schema slots are required (`Paper.sha256` + `Evidence.{paper, page, bbox, parser_name}` from T19's audit). The failing test was pivoted to `Evidence` missing `parser_name` (real `sh:minCount 1` on `palimpsest:parserName` per `shacl.ttl:328-332`). **Honest framing post-second-review:** the SHACL gate currently catches **zero** real violations from Pydantic-constructed inputs — every `sh:minCount 1` slot is also Pydantic-required, `sh:closed` is covered by `extra="forbid"`, cross-class `sh:class` is covered by `Optional[ConcreteClass]` typing. The gate's defensive value is for *future* code paths (external JSON-LD, T24 round-trip from oxigraph), not present-day catch. Two independent `reviewer` subagent passes (PASS_WITH_CAVEATS, 0 blockers each). **Pass 1 gaps cleared:** `test_closed_shape_rejects_unknown_property` (hand-crafted Condition with `fictional_slot` → `sh:ClosedConstraintComponent` violation, verified by inspecting the report text — confirms SHACL does work Pydantic can't at the RDF layer). Pass 1 also added `test_batch_returns_failure_in_place`, which **pass 2 correctly diagnosed as misnamed/redundant** (never actually called `validate_batch`, just duplicated `_validate_jsonld` from test 2) — deleted in this commit. A meaningful `validate_batch` mixed-pass/fail test requires a Pydantic-valid + SHACL-fail input that doesn't exist in the current schema; comment in `test_validation.py` documents the gap until F4 lands or T24 introduces an external JSON-LD path. **Reviewer-surfaced schema bug (now flagged blocks-T24):** `tasks/T18a-schema-cleanups.md:F4`: `bbox` as a repeated `palimpsest:bbox` predicate is RDF-dedup vulnerable. Real bboxes regularly share coordinates (single-line text spans with `y0 == y1` after rounding, axis-aligned regions) → collapse to 2–3 literals → trip `sh:minCount 4`. T24 will refuse a non-trivial fraction of valid extractions until F4 lands. Recommendation in F4 is option (b): 4 typed per-corner predicates. Pre-fixture pydantic probe (`{k: (v.default, v.is_required()) for k, v in cls.model_fields.items()}`) settled the `sh:closed=true` risk before writing the fixture — confirmed Overpotential has only the 4 Measurement-inherited slots with None defaults; the first fixture draft used `electrolyte_ph=1.0` on `Condition` but that slot lives on `Electrolyte` (caught by `pytest` on first run, fixed before commit). **Verification:** `pixi run pytest tests/test_validation.py -v` → 4 passed (3 card + closed-shape from pass 1); `pixi run pytest` → **82 passed / 3 live skipped** (was 78/3 after T22 polish, +4 from this file, 0 regressions). 5 rdflib `ConjunctiveGraph` DeprecationWarnings are upstream noise (not our code). **NOT touched per card:** `schema/generated/*` (T19 owns), `extract.py` (T22 sealed), `agent.py`/`cost.py`/`cache.py`/`provider.py` (T24 wires validation into insert). Total scope: 2 files modified, 1 file (`tasks/T18a-schema-cleanups.md`) extended with F4 blocks-T24 follow-up. (2026-06-08, `acc5279` + post-review polish)

- ✓ T18a F1+F2+F4 schema cleanups — single regen batch (`5233d45`). **F1:** `OxygenEvolutionReaction` left intentionally slot-less, with an inline YAML comment explaining the IRI-placeholder role so future maintainers don't think slots were forgotten. **F2:** 5 orphan slots removed from `slots:` — `overpotential_at_10mAcm2`, `activation_overpotential`, `anodic_overpotential` (typed Overpotential sub-slots; the values live on `value`+`unit_label`+`condition` instances), `catalyst`, `oer_reaction` (would-be relationship slots; re-add when an actual class needs them). Grep-confirmed none were referenced on any class. **F4:** `bbox` split from `multivalued: true, min/max 4` into 4 typed slots `bbox_x0`/`bbox_y0`/`bbox_x1`/`bbox_y1` on Evidence, each `range: float, required: true` (option (b) from T18a). Closes the T23-reviewer-surfaced RDF-dedup bug where SHACL `sh:minCount 4` on a single `palimpsest:bbox` predicate tripped for axis-aligned regions or single-line text spans (any bbox with repeated coordinates). Cascade kept surgical via pre-edit grep: `src/palimpsest/tools/extract.py` system prompt mentions 4 slots; `tests/test_schema_gen.py` required-fields test now exercises all 4; `tests/test_extract.py` 3 evidence fixtures switched; `tests/test_validation.py` fixture + hand-crafted JSON-LD switched and the stale F4-bug comment block updated to reflect the resolution; `skills/oer-extraction/SKILL.md` output-discipline section updated. Sanity post-fix: `Evidence(... bbox_x0=0.0, bbox_y0=0.0, bbox_x1=1.0, bbox_y1=1.0, ...)` (previously dedup-fatal degenerate case) now passes `validate_instance`. **F3 (missing Measurement subclasses Stability/PEMWECellVoltage/optional SpecificECSA/Pressure)** deferred to a fresh T18a session per user-approved scope choice — T20 SKILL.md already teaches the LLM to record these as free-text annotations, so no production blocker. **Pre-edit advisor probe** (analog of the T22/T23 fixture probes) ran `grep -rn "bbox" src/ tests/ skills/ schema/palimpsest.yaml docker/dots_run.py docker/paddle_run.py` — surfaced all cascade sites at once; `docker/dots_run.py` mentions parser-side bbox `[x1,y1,x2,y2]` but that's the parser's output format, NOT Evidence's bbox (intentionally untouched per CLAUDE.md §3). **Verification:** `pixi run linkml-validate schema/palimpsest.yaml` → No issues found; `pixi run schema` regenerates clean + 4 headers re-added; `pixi run pytest` → **82 passed / 3 live skipped** (was 82/3 after T23 post-review-2; 0 regressions). NOT touched: `src/palimpsest/validation.py` (the JSON-LD walker is field-name-agnostic, walks `model_fields`; the F4 split surfaced 4 leaf fields instead of 1 list field and the walker handled both correctly without modification). (2026-06-08, commit `5233d45`)

- ✓ T24 pyoxigraph insert with provenance triples — `src/palimpsest/store.py` (155 LOC) replaces the empty stub: `RDFStore` class with `__init__(path=None)` (in-memory default; pass a path for RocksDB on disk), `insert_extraction(instance, *, run_id, parse_run_id=None) -> str` (returns the minted `palimpsest:measurement/<uuid4>` IRI), and `sparql(query) -> list[dict]` (rows as `{var_name: literal_or_iri_string}`). **Signature deviation from card (documented in module docstring):** card prescribed 7 positional provenance args (`paper_sha, page, bbox tuple, parser_name, run_id, source_text`) but T19's audit + F4's split now make all 7 carried by `instance.evidence` (Pydantic-required), so re-passing duplicates work T22 already did; slimmed to `(instance, *, run_id, parse_run_id=None)` — `run_id` stays explicit because it's not a schema field (CostMeter ledger concern per card line 16). **Run_id decision (card line 16's explicit "DECIDE before coding"):** chose option B per advisor — extraction `run_id` lands as `palimpsest:runId` on the PROV-O activity, and the optional `parse_run_id` lands separately as `palimpsest:parseRunId` so a parse → triple chain is recoverable per-triple without joining through `parser_runs`. **Provenance non-negotiable enforced loudly:** `insert_extraction` raises `ValueError("refuse to insert ... without Evidence")` if `instance.evidence is None`, never silently drops or partial-inserts. **Triple shape per insert** (per the 18-LOC sanity test): measurement node `palimpsest:measurement/<uuid4>` gets `rdf:type` (EMMO IRI for Overpotential, palimpsest IRI for TafelSlope etc — resolved via `_class_iri` reading `linkml_meta.root["class_uri"]` and expanding the CURIE), `palimpsest:value`, `palimpsest:unitLabel`; paper node `palimpsest:paper/<sha256>` gets `rdf:type schema:ScholarlyArticle` + `palimpsest:sha256` + optional `schema:identifier` (DOI) + `schema:name` (title) — set-semantic so the same paper across multiple extractions writes once; PROV-O links `prov:wasDerivedFrom` (m → paper) and `prov:wasGeneratedBy` (m → activity blank node); activity blank node carries `prov:used` (paper), `palimpsest:page`, 4×`palimpsest:bbox{X0,Y0,X1,Y1}` (F4-aligned typed per-corner predicates, no dedup risk), `palimpsest:parserName`, `palimpsest:runId`, optional `palimpsest:parseRunId`, optional `palimpsest:sourceText`. **One pyoxigraph API quirk caught by tests** (not by docs): `QuerySolution` is positional `sol[i]` or var-name `sol[Variable("o")]`, NOT `sol.variables` — the `QuerySolutions` outer wrapper carries `.variables`. Probed before writing the SPARQL helper. **Second quirk:** xsd:float literals canonicalize ("236.0" → "236" round-trip); tests assert numerically (`float(rows[0]["v"]) == 236.0`) rather than pinning the textual form. **Third quirk caught at runtime, not on probe:** `linkml_meta` exposes `.root` not `.value` — first SPARQL test returned 0 rows because `_class_iri` was falling back to `palimpsest:Overpotential` instead of the EMMO IRI; fixed in same commit. **Verification:** `pixi run pytest tests/test_store.py -v` → 5 passed (insert + ≥15 triples, SPARQL value roundtrip, refuses without Evidence, two-run_id distinct predicates, RocksDB-path persists across `del s1 ; RDFStore(path)` reopen); `pixi run pytest` → **87 passed / 3 live skipped** (was 82/3 after T18a batch, +5 from this file, 0 regressions). NOT touched per card: `ontology.py` (T18 stable), `validation.py` (T23 stable). `store/` already gitignored. T25 will wire `extract → validate_instance → store.insert_extraction` end-to-end. (2026-06-08)

- ✓ T47 H2KG skos alignment — `schema/palimpsest.yaml` gains the `h2kg` prefix (`https://w3id.org/h2kg/hydrogen-ontology#`, v1.0.0) + `skos:closeMatch` (`close_mappings`) from all 7 OER metric classes to their official H2KG terms. **close NOT exact** (decided from primary evidence): H2KG terms are `h2kg:Property`/`h2kg:Parameter` **individuals**, ours are `owl:Class` Measurement subclasses — exactMatch would assert invalid class↔individual interchangeability. 3 local metrics (TafelSlope, MassActivity, TurnoverFrequency) gain an H2KG home; 4 ECHO-bound metrics dual-anchored (ECHO `class_uri` kept + h2kg close_mapping); ECSA → `h2kg:ElectrochemicallyActiveSurfaceArea` (spelled-out fragment, altLabel "ECSA"); OER comment notes the shared-upstream gap (only `h2kg:OERPerformanceDataset`, no native reaction class). **Fragment names + types verified against the live TTL** — the `.../releases/1.0.0` IRI 404s, content content-negotiates to `raw.githubusercontent.com/ViMiLabs/AIMWORKS/main/.../core_schema.ttl`; WebFetch's "not found" on 3 terms was truncation (real subjects at lines 7144/7154/18643). Regenerated `schema/generated/*` + re-added 4 headers; `shacl.ttl`'s large diff is gen-shacl blank-node reordering noise only. Tests: `test_schema_gen.py` (h2kg prefix in context.jsonld + each CURIE on the right class AND surviving into generated pydantic.py) + `test_ontology.py` (offline guard: no h2kg IRI in KNOWN_IRIS; **slow live guard**: fetch+cache H2KG TTL, assert all 7 fragments resolve with declared Property/Parameter type — the only test that catches a wrong/renamed fragment). Independent reviewer: PASS_WITH_CAVEATS, both test caveats addressed. **Verification:** `pixi run schema` exit 0; `grep -c h2kg context.jsonld` → 1; `pixi run pytest` → **102 passed / 3 skipped** (was 95/3, +7 live fragment tests, 0 regressions). NOT touched: `store.py`, `extract.py`, `ontology.py`, parallel metric slots. Re-grounds T43 against an official standard. (2026-06-11, `6809e5e`)

- ✓ T49 bbox from parser geometry + unit validation (C3+C2), Option B — Methodology fix from the 2026-06-10 review: the LLM no longer transcribes bboxes (which made **T38** measure LLM transcription, not parser localization). `extract.py` now (a) prompts the LLM for a verbatim `source_text` quote + page instead of bbox coords, and (b) resolves the 4 `bbox_*` corners from each parser's NATIVE geometry by whitespace/case-normalized substring-matching the quote against parser spans (union of matched spans on the stated page). 4 in-file adapters (mineru list[page]→block `bbox`+nested `content`; dots `pages[i][span].bbox+text`; paddle `res.parsing_res_list[].block_bbox+block_content`, page_index 0-based; docling `texts[].prov[].bbox{l,t,r,b BOTTOMLEFT}+page_no` 1-based) + chandra→no geometry. **Decision: Option B (minimal)** chosen by user after tracing that the card's implied null-bbox/`bbox_source` design (Option A) would cascade to ~8 files — relaxing `required:true` on `bbox_x0..y1` (T18a F4), regenerating `schema/generated/*`, guarding the unconditional `store.py:151-157` bbox insert (`str(None)`→poisoned `"None"^^xsd:float`), and breaking `test_schema_gen.py`/`test_store.py`. B keeps schema + store.py untouched: a measurement whose bbox can't be resolved (Chandra = no geometry, or no-match quote) routes to `errors`, never into the graph. **Invariant gained: every bbox in the graph is parser-native** → T38 now measures parser localization (C3 closed). No `bbox_source` slot stored (it'd be the constant "parser"). **Accepted deviations from card** (logged in T49 card + T38, no silent cap): Chandra excluded from the graph entirely (not just the metric); a Chandra extraction routes to `errors` "without crashing" rather than yielding `bbox_source="none"`; bboxes stored native-coords (docling BOTTOMLEFT/points vs others TOPLEFT/pixels) → T38 must normalize per-parser + flip docling-y before IoU, and exclude Chandra. **C2 (refined in live verify, user chose "Both"):** exact-string `==` rejected *correct* units in paper-faithful spelling, so `normalize.units_match()` compares unit **signatures** (unicode/LaTeX superscripts→exponents, `/`+`⁻¹`→denominator, strip spaces+`_Ir`, `dec`≡`decade`): `s⁻¹`≡`1/s`, `A g⁻¹_Ir`≡`A/g` pass; `V`≢`mV` (1000× error) still fails → `errors`. Prompt also gained ASCII-spelling examples. **Matcher:** `_norm` strips ALL whitespace (parsers pad inline equations: mineru `Ir- Co_{3}O_{4}` vs quote `Ir-Co_{3}O_{4}`); `_MIN_SPAN_MATCH_CHARS` guard stops short spans (docling `h`) polluting the union; tolerant `int(page)`. Files: `tools/extract.py` (293→522 LOC: 4 adapters + matcher + 2 error gates), `normalize.py` (+`canonical_unit`, `units_match`/`_unit_signature`, prompt line), `tests/test_extract.py` (cache seeds real mineru geometry; `_evidence` emits `source_text`; +4 tests incl. short-span pollution guard) + `tests/test_normalize.py` (`units_match`/`canonical_unit` unit tests). Doc-only: T49 + T38 cards. NOT touched: `store.py`, `schema/*`, `test_schema_gen.py`, `test_store.py`, `agent.py`, `cache.py`, `provider.py`. **Verification — offline:** `pixi run pytest tests/test_extract.py -v` → 14 passed/1 live skipped; full offline suite **115 passed / 8 skipped, 0 failures** (0 regressions); 5 adapters smoke-tested on real `cache/3432…/` (mineru 144/dots 148/paddle 163/docling 656 spans, pages 1–12; chandra 0). Matcher prefers the **tightest containing span**, unioning only when the quote is split across spans (offline test guards against a far same-page fragment inflating the bbox — matters for fine-grained docling). **Live** (real paper, user-authorized, **mineru only** — test globs `mineru.json`): 0 valid (units strict) → 8/5 (units fixed) → **final 14 valid / 0 errors**, every measurement got a parser-native bbox + valid unit. docling/dots/paddle adapters are span-level smoke-tested but NOT yet exercised end-to-end through the matcher. ⚠️ The live verify runs drained the palimpsest Anthropic account credit; top up before next live run (offline unaffected). (2026-06-12)

- ✓ T50 DeepSeek v4 flash as default LLM + T49 bbox loose ends (live-verified on DeepSeek) — **Cost pivot** (user): Anthropic Sonnet too expensive + account credit drained by T49 live runs → switch default LLM to **`deepseek-v4-flash`** (~$0.14/M in, $0.28/M out; a real full-paper extraction now costs **€0.0084** vs Sonnet's €0.29 — ~35×). **Key enabler (confirmed live):** DeepSeek serves an **Anthropic-wire-compatible endpoint** at `https://api.deepseek.com/anthropic` — works with the `anthropic` SDK, returns **Anthropic-shaped `usage`** (`input_tokens`/`output_tokens` — so `_cost_eur` is correct and the €50 cap holds), tool use works (agent loop verified), and `cache_control` is accepted-and-ignored. On that basis we reuse the existing `anthropic` SDK + ALL of `agent.py`/`extract.py` plumbing with **zero new deps** and **no agent-loop refactor**: `DeepSeekProvider(AnthropicProvider)` just repoints `base_url`+model (`src/palimpsest/providers/deepseek.py`); `AnthropicProvider.__init__` gained `base_url`/`name` params. Per-provider pricing: `_cost_eur(usage, prices=None)` defaults to Sonnet so stubs are unaffected; `DeepSeekProvider.prices` carries flash rates; call sites in `agent.py`+`extract.py` pass `getattr(provider,'prices',None)`. Defaults flipped to DeepSeek in `extract.py` (`_PROVIDER`) + `__main__.py`. **Two fixes surfaced by the first live runs:** (1) **extended thinking** — deepseek-v4-flash defaults thinking ON, spending a run-VARIABLE share of the output budget on reasoning tokens → JSON truncated unpredictably (a stub-only offline suite never sees this). Fix: `DeepSeekProvider.extra_request = {"thinking": {"type": "disabled"}}` merged into every request (provider-agnostic hook; verified it returns text-only). (2) `max_tokens` made configurable on the provider (`AnthropicProvider` 4096; DeepSeek 8192) since the T49 verbatim-quote prompt makes the JSON larger. **Schema-↔-prompt mismatch (user chose to fix properly):** the 5 `normalize.UNIVERSAL_ENUMS` (`iR_correction` etc.) were advertised to the LLM but never modeled — DeepSeek emits `condition.iR_correction` → Pydantic `extra_forbidden` → 0 valid (Sonnet happened to dodge it). Fix: modeled all 5 as LinkML enum slots on the `Condition` class (`schema/palimpsest.yaml` `enums:` block + slots), regenerated `schema/generated/*` + re-added the 4 headers; `test_universal_enums_match_schema` pins the schema enums in sync with `UNIVERSAL_ENUMS` (drift = the same bug returns). **This expands T50 scope into the schema** (the plan said "don't touch schema") — authorized by the user mid-task. **modeled→persisted (independent-review catch):** `store.py`'s `_add_condition` used a hardcoded `_COND_SCALARS` table, so the new enum slots validated but were silently DROPPED at insertion — defeating the whole point of modeling them. Added the 5 enum rows + made `_add_scalars` unwrap `Enum` → `.value` (LinkML str-enums stringify to `"IRCorrectionEnum.applied"` otherwise). Verified end-to-end via SPARQL (`palimpsest:iRCorrection → "applied"` in the graph) + pinned by `test_store.py::test_condition_enums_reach_graph`. **Q1 (LLM should not do bbox):** `_schema_without_bbox()` strips the 4 `bbox_*` slots from the Evidence def shown to the LLM so the contract is coherent (runtime resolves bbox from parser geometry; validation/store use the real schema unchanged). **Q4 (test other 3 parsers):** `test_per_parser_bbox_pipeline` runs dots/paddle/docling synthetic geometry through `_load_spans`→`_resolve_bbox`, asserting each adapter resolves the right bbox — the only feasible check for docling (4.86M tok) / paddle (217K), too big to feed extraction whole (flag: extraction-input chunking is a separate future task). **Tests:** `test_e2e` repointed to DeepSeek (gated on `DEEPSEEK_API_KEY`; also verifies tool-calling through the compat endpoint); `test_anthropic`/`test_agent` cache-specific tests stay Anthropic-gated (skip without `ANTHROPIC_API_KEY`); `test_live_extract` now DeepSeek + key-guarded. **Verification — offline:** plain `pixi run pytest` (real `.env`, no `--live`) → **122 passed / 7 skipped, 0 failures** (paid live tests, incl. the Anthropic-specific ones now marked `@pytest.mark.live`, skip without `--live` so a dead Anthropic key can't fail the suite). **Live (DeepSeek, user-authorized):** (a) `test_extract.py --live` → **6 valid / 5 errors, €0.0084** — real OER values (236/298/412 mV; Tafel 52.6/75.8/109.8 mV/decade), parser-native bboxes resolved, units + `iR_correction` validated (the 5 errors are quotes that matched no parser span — inherent matcher brittleness, honestly routed to errors); (b) `test_e2e --live` (agent loop + tool-calling through the compat endpoint) → **PASS** (read the PDF title via tools, billed the ledger). **DEVIATION from CLAUDE.md locked stack** ("Anthropic primary, DeepSeek fallback only"): user explicitly chose "replace Anthropic entirely". `AnthropicProvider` class KEPT as fallback (not deleted); runtime default is DeepSeek. CLAUDE.md updated (line 38) to record the DeepSeek default + endpoint + reason; deviation accepted by the user. Card backfilled at `tasks/T50-deepseek-default.md` (2026-06-15). (2026-06-13)

- ✓ T51 span-projection + ID-citation extraction (generalize parser input for any LLM; live-verified, committed `5d7dbb1`) — Replaces the T49 fuzzy bbox-matcher. **Root cause it fixes:** `extract()` dumped raw parser JSON (docling **4.86M tok**; paddle 216K > Sonnet) and asked the LLM to echo a verbatim `source_text` substring, which a fuzzy matcher then matched — brittle (DeepSeek dropped a LaTeX `\,` → no match → 6/11 vs Sonnet 14/14) and unscalable. **Fix:** project each parser's native geometry to numbered `(id, page, text, bbox)` spans (reusing the `_spans_*` adapters); show the LLM `[id] (p<page>) <text>` and have it return `evidence: {"spans": [id]}`; the runtime maps ids → union bbox + page + concatenated `source_text` (**`_resolve_spans`**, no fuzzy matching). The whole projection is **~18-25K tok for every parser** (docling 4.86M→25K, ~194×), so it fits one call in any model. **S2 adapter-completeness fixes (load-bearing):** `_content_strings` now also reads `math_content` so mineru `equation_interline` LaTeX is captured (was empty → uncitable); `_spans_docling` now projects the separate `tables` array (synthetic-tested; sample corpus has none). **No-strip policy:** span text shown + stored **verbatim** — LaTeX/subscripts (`_{Ir}`, `^{-1}`, `Co_{3}O_{4}`) carry chemical/unit meaning, never stripped. **Mis-citation guard:** if the value's digits aren't in the cited span, route to errors. **S4 — single call default + conditional per-page fallback** (above `_MAX_PROJECTION_TOKENS=120K`, global ids): the user first chose always-per-page, but **live data reversed it** — per-page gave wild variance (4 then 17 valid across runs, 12 calls), while one call over the full projection gave a consistent **11-13 valid** (1 call, €0.0041), full verbatim text, no stripping. Per-page's premise (need room to avoid stripping) was moot since the projection is only ~20K. **Yield root cause (independent-review catch):** the remaining DeepSeek shortfall (and run-to-run variance) was NOT the matcher — the LLM intermittently emits Condition numeric fields as unit-bearing strings (`current_density: "10 mA cm-2"`), which fail Pydantic `float` and discarded the WHOLE measurement (one run: 11 such errors → 5 valid). `_coerce_floats` salvages string→leading-float for the numeric Condition/Electrolyte slots → **stable 11/12/13 valid, 0 errors across 3 runs** (matches/exceeds Sonnet's 14). Also fixed `_dedup` to key on `source_text` so two catalysts reporting the same value aren't collapsed (advisor catch). **Superseded:** `_resolve_bbox`/`_norm`/`_bbox_area`/`_MIN_SPAN_MATCH_CHARS`/`_inject_provenance` removed. **Unchanged:** schema, store.py, normalize.py (units/enums), providers, agent. extract.py flat at 570 LOC. **Verification:** `ANTHROPIC_API_KEY="" pixi run pytest` → **125 passed / 7 skipped, 0 failures**; live DeepSeek (3 runs) → **11/12/13 valid, 0 errors, €0.0041 each**, parser-native bboxes + verbatim source_text (LaTeX intact). Files: `tools/extract.py` (rewrite of the resolution half + global projection + condition coercion), `tests/test_extract.py` (reworked to ID-citation), `tasks/T51-*.md`, note in `tasks/T49-*.md`. (2026-06-15)

- ✓ T52 temperature=0 + flash-vs-pro A/B vs ground truth + close T18a F3 (recall; committed `b1e1978`) — Three levers to make DeepSeek extraction reproducible, measured, and more complete. **(1) temperature=0:** added to `DeepSeekProvider.extra_request` (rides `complete()`'s `kwargs.update`); extraction ran at the default 1.0 (max random) → yield swung wildly. temp=0 **removes the wild swing but is NOT deterministic** — DeepSeek (MoE) still varies modestly at temp=0 (observed flash yield 16-20 across runs); pro is the steadiest. (Corrected an earlier "byte-identical/near-deterministic" overclaim that rested on one 2-run probe and was contradicted by later multi-run data.) **(2) Ground truth + A/B:** hand-read the cached paper PDF (`s41467-022-35426-8`, Ir-Co₃O₄ Nat Commun 2022) → `experiments/ground_truth.md`: **12 schema-modeled** measurements (4 Overpotential 236/298/412/511 mV, 4 TafelSlope 52.6/75.8/109.8/131.3, 2 MassActivity 3343.37/65.35, 2 TOF 1.665/0.0237) **+ 7 unmodeled** (3 SpecificActivity 0.098/0.035/0.01 mA/cm²_ECSA, 4 Stability 30/15/9/6 h) + out-of-scope R_ct/FE. `experiments/ab_extract.py` scores `extract()` (temp=0) for both models vs GT. **Pre-F3 (denom 12):** flash 11/12 recall (92%), 100% precision, €0.004, stable; pro 12/12 (100%), 92-100% precision, €0.013. **(3) Closed T18a F3 ground-truth-driven** (user choice, no speculative classes): added `SpecificActivity` (mA/cm2) + `Stability` (h) as `Measurement` subclasses (`schema/palimpsest.yaml`, palimpsest-local IRIs mirroring MassActivity/TafelSlope) + `UNIVERSAL_UNITS` entries; regen `schema/generated/*` + headers; `_CLASS_MAP` auto-discovers them, `store.py` persists generically (untouched); `SKILL.md` table updated (were free-text). NOT adding PEMWECellVoltage/SpecificECSA/Pressure (no paper reports them — CLAUDE.md no-speculative; left open in F3). **Post-F3 (denom 19):** the new types are now captured live — **pro 19/19 recall (100%) both runs**, €0.027; flash 16-19 (84-100%), €0.008. **Closing the schema gap lifted achievable recall 12→19.** **Recommendation (in `experiments/ab_results.md`): flash for dev, pro for final/thesis runs** (steady 100% recall at €0.027/paper); default left at flash (one-line switch). Caveat: one paper cached; multi-paper needs GPU parsing. **Verification:** `ANTHROPIC_API_KEY="" pixi run pytest` → **128 passed / 7 skipped, 0 failures**; `pixi run linkml-validate` + `pixi run schema` clean; A/B live runs as above. Files: `providers/deepseek.py` (temp), `schema/palimpsest.yaml`+generated (2 classes), `normalize.py` (2 units), `skills/oer-extraction/SKILL.md`, `experiments/{ground_truth,ab_results}.md` + `ab_extract.py` (new), tests (`test_extract`/`test_schema_gen`), `tasks/T52-*.md`, `tasks/T18a` (F3 partially closed). NOT touched: `store.py`, `agent.py`, `providers/anthropic.py`, parsers. (2026-06-15)

- ✓ T25 end-to-end pipeline (1 paper → graph + SHACL pass; first complete vertical slice) — `src/palimpsest/pipeline.py` (95 LOC) `run_paper(pdf_path, parser_name='mineru', skill_name='oer-extraction', *, store/cache/cost_meter/provider/run_id injectable) -> {paper_sha, n_extracted, n_validated, n_inserted}` wires the already-built, separately-tested pieces with **no new domain logic**: `parse_with_cache` (T16; all-5-parsers-cached → short-circuit, **no pod / no spend**) → `extract` (T22) → `validate_instance` (T23; SHACL failures dropped + logged) → `RDFStore.insert_extraction` (T24; provenance). `__main__.py` gains a `demo <pdf>` subcommand **before** the existing T08 agent path (lazy `run_paper` import to avoid import-time coupling; agent path left byte-identical). **Integration only — zero existing src modules touched.** The keyword-only injection kwargs mirror `extract`'s own convention so the test can query the store it passed in. **T46 dependency satisfied in substance** (PROGRESS has no standalone T46 line): `store.py` already writes the `palimpsest:condition`→`palimpsest:currentDensity` edge (cites T46b/C4 inline) — the C1 condition-retention work landed folded into T24/T50. **Card-vs-reality drifts handled, not bandaided** (per card line 53): (1) card SPARQL `?m a palim:Overpotential` is wrong — Overpotential is typed with its **EMMO ECHO IRI** (`store.py:137`), so the test resolves the real type IRI via `_class_iri(Overpotential.model_construct())`; (2) `extract` takes `paper_sha` (reads the cache itself), not a parser path, so `run_paper` passes the sha. `tests/test_pipeline.py`: **(a)** offline smoke — stub provider returns `{"items": []}` → asserts the full wiring runs and yields an all-zero summary + empty graph (covers import cycle, dict keys, parse-cache short-circuit) with no network/spend; gated on **all 5** parser outputs being cached so a partial cache skips rather than spinning a paid pod; **(b)** `@pytest.mark.live` acceptance — real DeepSeek. **Independent `reviewer` subagent caught a BLOCKING test defect (B1):** the C1 guard originally used **two independent** `store.sparql` calls binding `?m` separately — it would stay green even if the 236 mV Overpotential lost its condition while a *different* Overpotential kept one, i.e. it failed to guard the exact condition-dropping regression the 2026-06-10 amendment targets. Fixed to **one correlated query** (`?m a <Ov> ; value ?val ; condition/currentDensity ?cd`) so value AND condition bind to the same node; reviewer-verified it returns 236/298/412 mV all at `currentDensity=10`. Also tightened the offline-smoke cache gate from mineru-only to all-5 (the short-circuit precondition) so the "no spend" claim is enforced, not incidental. **Verification:** `ANTHROPIC_API_KEY="" pixi run pytest` → **129 passed / 8 skipped** (was 128/7 after T52; +1 offline smoke runs, +1 live skipped, 0 regressions); `pixi run pytest tests/test_pipeline.py -v --live` → 2 passed (live run 16–20 valid / all inserted, Overpotential within 10% of 236 mV + condition retained, **€0.0044**); `pixi run python -m palimpsest demo papers/s41467-022-35426-8.pdf` → prints `{…'n_inserted': 20}`. **Deferred (reviewer non-blocking, per CLAUDE.md no-speculative):** the SHACL-drop branch (`n_validated < n_extracted`) and the insert `ValueError`-skip branch are defensive and unreachable for Pydantic-valid, evidence-bearing inputs (extract already routes evidence-less/mis-cited items to `errors`), so they are logged-and-continue but not unit-tested. NOT touched: `extract.py`, `validation.py`, `store.py`, `schema/*`, `ontology.py`, `agent.py`, `cost.py`, `cache.py`, `providers/*`, `parsers/*`. (2026-06-15)

## Week 4 — UI & viewer

- ✓ T26 Textual chat TUI skeleton — `src/palimpsest/tui/app.py` (`PalimpsestApp(App)` + `main()`): top cost-meter bar (`Static#costbar`, `€{total_eur:.2f} / €{cap:.0f}`), scrollable `RichLog#log`, bottom `Input#prompt`; `on_input_submitted` → `/`-prefix writes a "land in T27" stub (never calls the agent), else launches a thread worker; `ctrl+q` quit, `ctrl+l` clear. `tui/__main__.py` + `styles.tcss` (costbar dock top, log 1fr, input dock bottom) + pixi `tui = "python -m palimpsest.tui"`. **Concurrency (the design crux, advisor-confirmed Option B):** `agent.run()` makes a multi-second network call AND writes to CostMeter — running it on Textual's main-thread event loop freezes the UI every message, so it runs in `@work(thread=True)`. That required `cost.py` (sealed since T05) → `sqlite3.connect(..., check_same_thread=False)` (one strictly-more-permissive line + invariant comment), since the default `check_same_thread=True` raises `ProgrammingError` from a worker thread (a stub smoke test wouldn't catch it). **Serialization is by construction, NOT `exclusive=True`** (cancelling a Textual *thread* worker doesn't kill the OS thread): the `Input` is disabled while a request is in flight (single-in-flight) and the cost bar refreshes via `call_from_thread` *after* the worker returns (sync barrier) → CostMeter never accessed concurrently → no `threading.Lock` needed. **Deviations from card (logged in DEVIATIONS.md):** (1) the CostMeter edit; (2) `main()` wires `DeepSeekProvider` only (T50 default) — NOT the card's `AnthropicProvider + ParserCache + RDFStore + SkillLoader`; `Agent` doesn't take the latter three (tools build their own deps), so constructing them would be speculative (CLAUDE.md §2). User text + agent replies are `rich.markup.escape`d so typed `[..]` isn't parsed as markup. **Independent `reviewer` subagent: PASS_WITH_CAVEATS, 0 blockers** — tried to break the concurrency claim and couldn't (`on_input_submitted` is a sync handler, runs atomically, disables the input before any second `Submitted` can dequeue). Reviewer's one named-card-requirement gap fixed pre-merge: the stub agent now bills the meter so `test_app_smoke_reply_path` asserts the cost bar moves (`0.01` in the rendered `#costbar`); also added an error-path test (raising `agent.run()` → `error:` line + input re-enabled, no crash). Non-blocking (deferred): shutdown-mid-flight raises teardown noise (`call_from_thread` on a stopped app; no data loss, MVP-acceptable); `SYSTEM_PROMPT` duplicated with `__main__.py` (shared-constant refactor for a later task). **Verification:** `ANTHROPIC_API_KEY="" pixi run pytest` → **132 passed / 8 skipped** (was 129/8 after T25; +3 TUI tests, 0 regressions); `pixi run pytest tests/test_tui.py -v` → 3 passed (offline, stub agent, async pilot via `asyncio.run` since no `pytest-asyncio`). Live `pixi run tui` (needs `DEEPSEEK_API_KEY`) not run in-session. NOT touched: `agent.py`, `tools/*`, providers/store/cache/pipeline. (2026-06-15)

- ✓ T27 slash command dispatcher — `src/palimpsest/tui/slash.py` (49 LOC): `SLASH_COMMANDS: dict[str, Callable]` = `{"help": _help, "quit": _quit}` + `dispatch(app, line) -> str` (first-token string match, no regex per card; `line.lstrip("/").split()`, args passed to the handler but ignored for now). `/help` lists each command as `/{name} — {handler.__doc__}` so the registry stays the card's typed `dict[str, callable]` (descriptions live in docstrings, not dict values); `/quit` calls `app.exit()` and returns `"bye"`; unknown → the card's exact `f"unknown command: /{cmd}. type /help for available commands."`. `tui/app.py` (edit): the `text.startswith("/")` branch in `on_input_submitted` now `log.write(escape(dispatch(self, text)))` (replaced the T26 "land in T27" stub) before the agent path — slash commands never reach the LLM, cost €0; stale module-docstring line updated. **TDD throughout** (skill), every test watched fail first. `tests/test_slash.py` (new, 4 tests): /help lists names AND descriptions, /quit exits a `_FakeApp`, unknown returns the exact string. **4th file beyond the card's 3 (logged in DEVIATIONS.md):** `tests/test_tui.py` — T26's `test_slash_command_stubbed` pinned the removed stub (`assert "T27"`), so it was rewritten to `test_slash_command_dispatched` (asserts unknown-command for the T28-deferred `/budget`, agent not called) + 2 added in-app pilot tests covering the card's manual `pixi run tui` step headlessly: `/help` renders the list, `/quit` exits (asserts `app._exit is True` + the post-exit `"bye"` write doesn't crash — the integration risk advisor flagged). **Independent `reviewer` subagent: PASS, 0 blocking**; took its one assertion-gap catch (the /help test verified names but not the card-required descriptions). **Scope respected:** `agent.py` untouched; `/budget`/`/cost`/`/model` deferred to T28; viewer/notebook commands not added. **Verification:** `pixi run pytest tests/test_slash.py -v` → 4 passed; `ANTHROPIC_API_KEY="" pixi run pytest` → **137 passed / 8 skipped** (was 132/8 after T26; +5, 0 regressions). Interactive `pixi run tui` (needs TTY + `DEEPSEEK_API_KEY`) not run in-session — covered by the pilot tests. NOT touched: providers, store, cache, pipeline, schema, cost.py. (2026-06-15)

- ✓ T28 /budget, /cost, /model slash commands — `src/palimpsest/tui/slash.py` (+3 handlers in the T27 `SLASH_COMMANDS` registry, ~50 LOC): `/budget N` int-parses then calls `cost_meter.set_budget(N)` (T05 — does the `N<spent` refusal + live DB write), returning the card's `budget cap → €N (spent …, headroom …)` on success / the refusal string otherwise / `usage: /budget <int euros>` on a non-int; `/cost` returns total + `llm €… / gpu €…` breakdown + last-10 ledger tail, reading the **already-public** `app.cost_meter.conn` directly (`GROUP BY kind` + `ORDER BY rowid DESC LIMIT 10` — rowid not ts, which is 1s-resolution and ties) so **`cost.py` stays untouched** per the card's "Will NOT touch"; `/model X` switches `app.agent.provider`. **Card deviation (user-approved via AskUserQuestion, logged in DEVIATIONS.md):** the card (lines 14-16, pre-T50) says "sonnet-only MVP; deepseek/haiku/gemini not implemented" — but T50 made DeepSeek the runtime default and both `DeepSeekProvider`+`AnthropicProvider` are real classes, so `/model` wires **both** (`sonnet`→Anthropic, `deepseek`→DeepSeek, each reconstructs+reassigns the provider, returns `switched to {name} (prompt cache reset)`), `haiku|gemini`→`provider 'X' not implemented yet`, unknown→`unknown model: X. options: sonnet, deepseek`; construction wrapped in try/except so a missing API key surfaces as `could not switch to X: …` not a TUI crash. `agent.py` also untouched (just reassigns the existing `.provider` attr). **Two pre-existing tests retargeted** (`tests/test_slash.py` + `tests/test_tui.py` both used `/budget` as their "unknown command" probe — implementing `/budget` makes those assertions false): switched to `/parser` (out of scope per card), preserving intent (slash routed to dispatcher, agent never called). `tests/test_tui.py` is thus a touched file beyond the card's stated 2 — same unavoidable-stale-test situation as T27. **TDD** (skill): 9 offline tests in `tests/test_slash_budget_cost.py` (new) watched fail first — `/budget` raise/refuse/non-int, `/cost` populated + empty-ledger (no "last entries:" header), `/model` switch (monkeypatch `slash._PROVIDERS` dict, not the module symbol — the dict captured class refs at import) + construction-failure-is-friendly + not-implemented + unknown. **Independent `reviewer` subagent: PASS_WITH_CAVEATS, 0 blocking**; applied both worthwhile NBs (this DEVIATIONS entry + the empty-ledger `/cost` test). **Verification:** `pixi run pytest tests/test_slash_budget_cost.py -v` → 9 passed; `ANTHROPIC_API_KEY="" pixi run pytest` → **146 passed / 8 skipped** (was 137/8 after T27; +9, 0 regressions). Manual `pixi run tui` smoke (`/budget 75`, `/cost`, `/model haiku`) not run in-session — needs a TTY; covered by the headless tests. NOT touched: `cost.py`, `agent.py`, `providers/*` (no new provider built — both already exist), `app.py`, schema, store, extract, pipeline. (2026-06-15)

- ✓ T29 FastAPI viewer skeleton + PDF.js — `src/palimpsest/viewer/app.py` (113 LOC) exports `app: FastAPI` with the card's 4 routes: `GET /health` → `{"ok": True}`; `GET /` lists papers in store (inline HTML, no separate home template — keeps the file set to the card's list); `GET /paper/{sha}` → `viewer.html` (404 if sha unknown); `GET /paper/{sha}/pdf` → `FileResponse(..., media_type="application/pdf")` (404 if absent). `viewer/templates/viewer.html` (Jinja2): two-pane CSS grid (PDF | data), PDF.js vendored ESM rendered manually into per-page canvases, `GlobalWorkerOptions.workerSrc` → vendored worker, HTMX from CDN (card line 18), empty `<div id="data-pane">` for T30. `viewer/static/pdfjs/` vendors **2 files** (`pdf.min.mjs`, `pdf.worker.min.mjs`, pinned cdnjs 4.10.38 + `VERSION.txt`) — not the card's "~10": the manual render path never uses PDF.js's prebuilt viewer UI, so its assets would be dead weight (CLAUDE.md §2). `pixi.toml` +`viewer = "uvicorn palimpsest.viewer.app:app --reload --port 8765"`. **Key reconciliation (DEVIATIONS):** the `papers` table stores `filename="sample.pdf"` (T07 fixture artifact) but `papers/` on disk holds DOI-named files with no `sample.pdf` — the DB filename can't locate the bytes. Resolved with `_pdf_index()` (`@lru_cache`) hashing every `papers/*.pdf` via T07 `read_paper`, so the bytes served for `{sha}` hash to exactly `{sha}` (provenance-true) and `cache.py` stays untouched. No new pixi dep: `fastapi`/`uvicorn` pinned, `jinja2 3.1.6` transitive via linkml (PyYAML/T21 precedent). **Independent `reviewer` subagent: PASS_WITH_CAVEATS, 0 blocking** — tried path-traversal (sha is only ever a dict key → 404), missing-DB (`_papers_in_store` guards `exists()` + catches `OperationalError` → `[]`, no stray DB file), empty papers dir (→ "no papers in store"), route collision (`/paper/{sha}` vs `/paper/{sha}/pdf` distinct) — all held. Applied reviewer NB-1 (real defect): one malformed file in `papers/` would 500 every route (and `@lru_cache` doesn't cache exceptions → re-hash each request) → `_pdf_index` now `try/except`-skips unreadable files, matching `_papers_in_store`'s defensive style. **Verification:** `ANTHROPIC_API_KEY="" pixi run pytest tests/test_viewer.py -v` → 7 passed (health / home / 2×404 / vendored-pdfjs-served / malformed-pdf-graceful / real-PDF-stream+template-wiring); full suite **153 passed / 8 skipped** (was 146/8 after T28; +7, 0 regressions). **Live-verified end-to-end:** `uvicorn` + curl (`/health`=`{"ok":true}`, home lists the cached paper, `/paper/<sha>/pdf` → 200 `application/pdf` 2.5 MB `accept-ranges: bytes`, `/static/pdfjs/pdf.min.mjs` 200, bad sha 404); **real browser (Playwright):** `/paper/3432d049…` rendered **all 12 pages → 12 canvases** (833×1107 @ scale 1.4), two-pane grid 600|600 px, data-pane empty; only console error was the benign auto `/favicon.ico` 404. `tests/test_viewer.py` is a file beyond the card's 4 (per-task test discipline, DEVIATIONS-logged). NOT touched: `agent.py`, `tui/`, `cache.py`, pipeline/extract/store/validation/cost/providers/parsers/schema/ontology. (2026-06-15)
- ✓ T29 follow-on — vendored PDF.js `cmaps/` (169 files, 1.6 MB) + `standard_fonts/` (16 files, 800 KB) from the same pinned 4.10.38 dist zip, and wired `cMapUrl`/`cMapPacked:true`/`standardFontDataUrl` into `getDocument()` so CID/CJK-encoded PDFs and PDFs that don't embed their fonts render correctly across a wider corpus (the demo paper is Latin+embedded-fonts so it already rendered; this is defensive for real-corpus robustness, raised in a post-T29 design Q with the user). Vendored total 4.1 MB. `test_vendored_pdfjs_is_served` extended to assert a cmap (`78-EUC-H.bcmap`) + a font (`FoxitDingbats.pfb`) serve 200. Re-verified in browser: 12 pages still render, **0 console warnings** (only the benign favicon 404). Full suite **153 passed / 8 skipped** (no count change — assertions added to an existing test, 0 regressions). NOT a new task; T29 scope (the PDF must render). (2026-06-16)

- ✓ T30 /paper/{sha}/data JSON endpoint (+ graph-persistence fix the card assumed) — `src/palimpsest/viewer/app.py` gains `GET /paper/{sha}/data`: one SPARQL SELECT joining each measurement to its Evidence → JSON `{sha, triples:[{id, slot_path, value, unit, page, bbox:[x0,y0,x1,y1], parser_name, source_text}]}`. Read-only over the graph, no LLM/spend. `slot_path` is synthesized as the friendly class name by reverse-mapping the measurement `rdf:type` IRI back to its Python class (`_type_names()` over all 9 generated Measurement subclasses) — EMMO-bound classes store opaque hash IRIs otherwise. `value`+bbox coerced to float, `page` to int; value/unit/sourceText queried `OPTIONAL` (store skips None). sha hex-guarded before interpolation into the SPARQL IRI; unknown/non-hex sha → 200 + empty `triples` (HTMX empty-state, not 404 like sibling routes). Store opened on-disk via `_graph_store()` (`RDFStore("store")`, lru_cache; RocksDB single-writer — viewer holds the lock, so populate with viewer stopped). Imports `PALIM`/`RDFStore`/`_expand` from `store.py` read-only (card's "Will NOT touch store.py" respected — no edit). **Persistence gap found + fixed (beyond card, user-authorized):** nothing committed wrote the graph to disk — `pipeline.run_paper`/`demo` used an in-memory `RDFStore()` discarded on exit, so the viewer's `store/` was always empty. `__main__.py` `demo` now passes `store=RDFStore("store")` (matches `viewer.app.STORE_PATH`); `run_paper`'s default stays in-memory so tests/embedders are unaffected. New offline regression `test_demo_cli_persists_to_disk_store` (fakes `run_paper`+`RDFStore`, asserts the CLI constructs a path-backed store) guards it. Independent `reviewer` subagent: **PASS_WITH_CAVEATS, 0 blocking** — verified SPARQL predicates match `store.py` exactly, the type map is exhaustive over all 9 subclasses, OPTIONAL handling correct, hex guard injection-safe; applied its top non-blockers (value-less-measurement test, two-paper-filter test, lock + 200-not-404 doc comments). **Live-verified:** `demo papers/s41467-022-35426-8.pdf` (DeepSeek, €0.0043, parsers cached → no GPU) → 18 extracted/validated/inserted → `pixi run viewer` + curl `/paper/<sha>/data` returned all 18 with every field, EMMO hash IRIs resolved to friendly names. **Honest gap surfaced (not a T30 bug, logged to T31 card):** MinerU's bboxes are block-level — 5 distinct boxes back all 18 values (8 share one), so provenance is coarse; T31 should back the MVP viewer on docling (finest spans + coords matching `convertToViewportRectangle`). Tests: `tests/test_viewer_data.py` (5) + `tests/test_pipeline.py` (+1). `ANTHROPIC_API_KEY=""` (empties a dead Anthropic key so Anthropic-gated tests skip) `pixi run pytest` → **159 passed / 8 skipped**, 0 regressions (was 153/8 after T29). NOT touched: `store.py`, `extract.py`, `pipeline.py` (only `__main__.py` demo wiring), schema, providers. T31 card extended with forward-compat constraints for the deferred multi-parser bbox toggle. (2026-06-16)

## Week 5 — experiments & writing
