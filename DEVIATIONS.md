# Deviations log

Every time Claude does something unexpected — good or bad — log it here. This becomes the reflection chapter of your thesis.

Template per entry:

```
## YYYY-MM-DD — T##
**What:** <what Claude did>
**Verdict:** <rejected/accepted/partially>
**Lesson:** <one sentence>
```

---

## 2026-05-26 — T01
**What:** `pymupdf4llm` has no conda-forge build, so the `pixi install` solver failed on osx-arm64. Moved it from `[dependencies]` to `[pypi-dependencies]` (per card note: prefer conda-forge, fall back to pypi if no arm64 build). `pymupdf` itself stayed on conda-forge.
**Verdict:** accepted
**Lesson:** Not every package the design lists for conda-forge actually ships there; verify at install time.

## 2026-05-26 — T01
**What:** Added `palimpsest = { path = ".", editable = true }` to `[pypi-dependencies]` — not literally in the card spec. The card's verification (`import palimpsest`) needs the src-layout package installed; this self-editable reference is pixi's standard mechanism for that.
**Verdict:** accepted
**Lesson:** src-layout + a verification that imports the package implies the project must self-install; the card omitted the line that makes it work.

## 2026-05-26 — T01
**What:** Added two things to `pyproject.toml` not in the card spec: a `[build-system]` table (`requires = ["setuptools"]`, setuptools build backend) and `requires-python = ">=3.11"`. The build-system table is load-bearing — PEP 517 editable install of the src-layout package (deviation above) needs it. `requires-python` agrees with the pixi `python = "3.11.*"` pin.
**Verdict:** accepted
**Lesson:** A pyproject that gets pip-installed editable needs a build-system table; the card's `[project]`-only spec was incomplete.

## 2026-05-26 — T08
**What:** Added a second tool `read_first_page_text(path)` (`fitz.open(path)[0].get_text()`) plus its registration and a mention in the `__main__` / e2e system prompts — beyond the card's two named files. With only `read_paper` (metadata: SHA-256/pages/size), the model correctly refused to invent a title; the literal "output contains the title" check couldn't pass. The card pre-authorizes exactly this tool as "acceptable scope expansion" and asks to log it here. User confirmed adding it over accepting the metadata-only answer. CLI now returns the real title ("Iridium single atoms incorporated in Co₃O₄ efficiently catalyze the oxygen evolution in acidic conditions", Nat. Commun. 2022).
**Verdict:** accepted
**Lesson:** A metadata-only tool can't satisfy a text question, and an honest model refuses rather than hallucinates — the card anticipated this and shipped the escape hatch with the task.

## 2026-05-26 — T09
**What:** The card's second verification snippet does `r.json().get("data", [])`, but `GET https://rest.runpod.io/v1/pods` returns a **bare JSON list**, not `{"data": [...]}`. The snippet crashes (`AttributeError: 'list' object has no attribute 'get'`) on the pod-count line — *after* the auth assert (`status_code == 200`) has already passed. Ran a shape-tolerant variant to confirm: status 200, 0 pods (test pod terminated). Pass condition (key accepted → 200) is met; only the cosmetic count line was buggy.
**Verdict:** accepted (card snippet had a latent bug; auth verification itself succeeds). **Fixed:** card snippet updated to `body if isinstance(body, list) else body.get('data', [])`; now exits 0 literally.
**Lesson:** The RunPod REST `/v1/pods` response shape is a top-level array; don't assume a `data` envelope. The card's count line was never load-bearing — 200 is the real gate.

## 2026-05-26 — T09
**What:** Used **RTX 3090** (Community Cloud) for the manual SSH connectivity test instead of the RTX 4090 named in the card, because the 4090 showed "Low" availability at deploy time. Card explicitly permits "any small GPU" for this test.
**Verdict:** accepted
**Lesson:** Availability on Community Cloud is variable; the connectivity path is GPU-agnostic, so substitute freely for a throwaway test. The 4090 is only load-bearing later (T10/T14) for the actual parsers.

## 2026-05-26 — T10
**What:** The card pins `FROM nvidia/cuda:12.1.0-devel-ubuntu22.04`. Current vLLM (0.19.1, released 2026-04-18) ships cu129 prebuilt wheels and dropped CUDA 12.1 support — torch/vLLM will not load on a 12.1 base. Bumped the Dockerfile base to **`nvidia/cuda:12.9.1-devel-ubuntu22.04`** and install torch + **`vllm==0.19.1`** from the cu129 PyTorch index (vLLM's documented default), so base runtime + torch wheel + vLLM all agree on CUDA 12.9 — enforced by a build-time `assert '+cu129' in torch.__version__`. Also dropped flash-attn (HF model card marks it optional; not named in the T10 card; saves a long nvcc build), and added a GitHub Actions workflow (`.github/workflows/build-gpu-image.yml`) as the build host since there is no local GPU/Docker — a 4th file beyond the card's three. User approved the base bump and the CI setup before code was written.
**Verdict:** accepted
**Lesson:** Pinned base-image / CUDA versions in a task card written months earlier go stale fast against a fast-moving dep like vLLM; check the dep's current CUDA support before honoring the literal pin.

## 2026-05-27 — T10
**What:** The first GitHub Actions build pushed fine to Docker Hub but RunPod refused to start the pod: `failed to pull image: layers from manifest don't match image configuration`. Cause: `docker/build-push-action@v6` (buildx ≥0.10) attaches a provenance attestation by default, publishing a multi-manifest OCI index (image manifest + in-toto attestation manifest). RunPod's image puller walks the index, hits the attestation entry whose config is not an image, and the diff_ids mismatch. Docker Hub renders the index fine, so the image *looked* healthy. Fixed by adding `provenance: false` + `sbom: false` to the build step so buildx pushes a plain single-image manifest; re-ran the workflow on the same tag.
**Verdict:** accepted (external-tooling default, not a Dockerfile bug)
**Lesson:** A "successfully pushed" image on Docker Hub is not proof it will run — buildx attestations make an OCI index that some container runtimes (RunPod) reject; for BYO-container targets, build with `provenance: false`.

## 2026-05-27 — T10
**What:** The card specifies `CMD ["bash"]` ("interactive — gpu_provider will SSH in"). On RunPod the container starts detached, so `bash` as PID 1 hits EOF and exits immediately. We first saw `Container ... is not running` and attributed it to this; the next attempt's system log revealed the early failures were at least partly the CUDA-12.9 nvidia init hook (see next entry), which blocks container creation *before* PID 1 regardless of CMD. Both fixes were still needed: on the working 12.8 host the container stays up only because `CMD` is now `["sleep", "infinity"]` (a bare `bash` would have exited there too). Immediate unblock used the RunPod template's *Container Start Command* override (`sleep infinity`); the Dockerfile change bakes it in.
**Verdict:** accepted (card's CMD is platform-incompatible)
**Lesson:** A RunPod custom container needs a long-lived foreground PID 1 (`sleep infinity`); `CMD ["bash"]` only works when a TTY is attached, which a detached pod start does not provide.

## 2026-05-27 — T10
**What:** Corrected the CUDA base from **12.9.1 → 12.8.1** (and torch/vLLM cu129 → cu128). The 12.9 image failed at the nvidia container-init hook on RunPod: `unsatisfied condition: cuda>=12.9, please update your driver`. Measured RunPod's available host drivers via the deploy page's CUDA-version filter: **ceiling is 12.8** (2× RTX 3090 available). 12.8 is also vLLM 0.19.1's lowest supported CUDA and the version olmOCR's official Docker image ships, with Chandra needing only 12.1+ and MinerU on torch cu128 — so all four parsers (T11-T13) share the 12.8 base. The earlier 12.9 choice (preceding entry) maximized version-cleanliness but ignored the binding constraint: the host driver caps the runnable CUDA, and I had not measured it.
**Verdict:** accepted (corrects the 12.9 over-reach with host data)
**Lesson:** The GPU **host driver** — not vLLM's default, not the parsers — is the binding constraint on CUDA version; measure the target hosts' supported CUDA *before* pinning a base image. The build-time `+cuXXX` assert proves image-internal consistency, never host compatibility.

## 2026-05-28 — T12
**What:** `runpod-template.md` first documented the olmOCR pod's Container Start Command as `-c "sleep infinity"`, reasoning that the upstream image's `ENTRYPOINT ["/bin/bash"]` would receive it as arguments (the way `docker run … olmocr -c "olmocr …"` does). On the first pod the container exited immediately — proxy SSH authenticated but reported `container … is not running`. RunPod **shell-wraps** the Container Start Command (`sh -c "<field>"`) instead of appending it to the image ENTRYPOINT, so `sh -c '-c "sleep infinity"'` tried to execute a command literally named `-c`. Corrected to bare `sleep infinity`; template + PROGRESS updated. (Model verification — `--help`, baked weights path, FP8 init — still deferred: no GPU available when tried.)
**Verdict:** accepted (corrected my own wrong instruction, empirically rather than from docs)
**Lesson:** RunPod's "Container Start Command" is run **through a shell**, not appended to the image's ENTRYPOINT — give it a bare command (`sleep infinity`), never Docker ENTRYPOINT-style args (`-c "…"`). Same container-liveness need as T10's `CMD ["sleep","infinity"]`, opposite-looking mistake: T10 was the image's baked CMD, this is the platform's field semantics.

## 2026-05-28 — T13
**What:** Three Chandra facts in the T13 card were stale/wrong against the live sources (PyPI `chandra-ocr` 0.2.0 / HF model card / DeepWiki), verified before writing the Dockerfile because Chandra is post-cutoff. (1) **Weights repo:** card said `datalab-to/chandra`; the package's default `MODEL_CHECKPOINT` is `datalab-to/chandra-ocr-2` (the v2 model — `datalab-to/chandra` is the *original*). Baked `chandra-ocr-2`, else the pod would re-download at runtime, defeating the bake. (2) **Verification command:** card said `chandra --version`; no `--version` flag exists (3 sources) — used `chandra --help` + `pip show chandra-ocr` + a baked-weights-path assert, mirroring olmOCR's T12 `--help` fallback. (3) **Param count:** card said 4B; HF card says ~5B (BF16). The 5B/BF16 fact also set the GPU recommendation to RTX 4090 / 3090 (Ampere OK), not Ada-only like olmOCR's FP8. Also strengthened the build-time assert to `import torch, vllm` (chandra serves via `--method vllm`, so a broken vLLM is this parser's runtime failure mode — catch in CI, not on a paid pod). **[Superseded by the next T13 entry: that `import vllm` is exactly what caught the wrong backend in CI. The final image uses the HuggingFace backend (`--method hf`) and asserts `import torch, torchvision, transformers`.]**
**Verdict:** accepted (card metadata stale vs live package; corrected from authoritative sources)
**Lesson:** A task card written before a post-cutoff release will name the wrong repo, flag, and size — verify the package's *current* default checkpoint, CLI surface, and model card before baking weights, since a wrong bake costs hours of CI to redo.

## 2026-05-28 — T13
**What:** The first CI build of the Chandra image (commit d0d634b) failed at the build-time assert: `ModuleNotFoundError: No module named 'vllm'`. The card's premise "Chandra brings its own torch/vLLM (not pinned)" is wrong — base `pip install chandra-ocr` 0.2.0 installs only the OpenAI-API **client** (openai, httpx, pypdfium2, markdownify, pydantic…); it bakes **no model runtime** (no vllm, no torch, not even numpy — the build log's `Successfully installed` list confirms it). Chandra's "vLLM backend" means it talks to a *separately run* vLLM OpenAI server, and its `chandra_vllm` helper launches a **Docker container**, which doesn't fit a single RunPod pod container. The `import vllm` I had added to the cu128 assert (prior session, on advisor's suggestion, as a runtime-contract check) is exactly what surfaced this — for free, in CI, instead of on a paid pod. Per user decision, switched the image to the **HuggingFace backend**: `pip install "chandra-ocr[hf]"` (torch + transformers, in-process), run `chandra … --method hf`; the assert is now `import torch, torchvision, transformers` (all import GPU-free, so they are safe in the no-GPU CI builder; torch and torchvision are both `+cu128`-checked — torchvision is the `[hf]` dep most likely to resolve off the cu128 index and split the CUDA ABI, caught in independent review).
**Verdict:** accepted (card's runtime assumption was wrong; corrected the design, caught at zero cost in CI rather than on a paid pod)
**Lesson:** "pip install <tool>" does not imply the tool can run a model — some OCR packages ship a thin API client and expect you to bring the server/runtime. Read the `Successfully installed` list, not the backend's name. A build-time `import <runtime>` assert earns its keep: it converted a would-be paid-pod failure into a free red CI build.

## 2026-05-29 — T13
**What:** Pod-verified the corrected Chandra image (CI build went green, pulled cleanly). On the pod: `cuda avail: True`; `torch 2.11.0+cu128` + `torchvision 0.26.0+cu128` (the review's torchvision `+cu128` guard held on real hardware) + `transformers 5.9.0`; `chandra --help` runs with `--method [hf|vllm]`; baked weights complete at 9.9 G (`config.json` + a single `model.safetensors`). Two notes: (1) RunPod assigned an **RTX PRO 6000 Blackwell (96 GB)**, not the 3090/4090 the template recommends — any BF16 GPU runs it, but it's far pricier, so it's a verify-and-tear-down card, not the one for the T16 corpus run. (2) Per user, the **live PDF parse is deferred to T16**, where each parser is invoked from code and compared — so T13 is marked ✓ on image + CLI + baked-weights + cu128-deps, the same bar T10 (docling) was marked ✓ on, not on a manual extraction.
**Verdict:** accepted (verification scoped to the card's bar; extraction validated later in T16, by design)
**Lesson:** "Is the model loaded on the GPU?" has no answer at rest for an in-process (HF) backend — nothing occupies VRAM until a parse runs, so an idle `nvidia-smi` reading 0 MB is healthy, not broken. Verify image/weights/deps on the pod; prove the actual inference where the model is invoked from code.

## 2026-05-29 — T14
**What:** Two user-approved scope adds beyond the card's "Will touch" (3 files → 5), plus gap-fills for parts the card left undefined. (1) **`tests/conftest.py` + a `--live` flag** — the card's own verification command (`pytest … --live`) needs a `pytest_addoption`, which only lives in a conftest, yet the card didn't list one. Asked the user; they chose the `--live` flag over mirroring the repo's existing `@pytest.mark.slow` + `skipif(env)` idiom (it's reusable by T16+'s live tests). Conftest scoped to register **only** the `live` marker and skip it unless `--live`; the pre-existing unregistered-`slow` warning in `test_e2e.py` was deliberately left as-is (out of scope). (2) **`fabric` added to `pixi.toml`** — the card offered "fabric *or* plain `subprocess`"; user chose fabric. Ephemeral pods rotate host keys, so SSH host-key checking is disabled via paramiko `AutoAddPolicy()` on the fabric `Connection`. (3) **Local `EUR_PER_USD = 0.92`** in `gpu_provider.py` mirrors `agent._USD_TO_EUR` by value — kept local per the card's "separate FX snapshot", so a parser never imports from the agent. (4) Filled three card under-specs (decisions, not departures, surfaced by an independent advisor pass): the undefined "pending work flag" → a public `self.pending_work` bool T16 toggles around a corpus pass; the watchdog↔`__exit__` race → an idempotent single-billing `_teardown` guarded by a `threading.Lock` (whichever fires first bills once, the other no-ops); "RUNNING ≠ sshd accepting" → a 10×3s connect-retry loop on first SSH. An independent review then caught a real billing bug: the watchdog billed from *its own thread*, but `CostMeter`'s SQLite connection is thread-affine (`check_same_thread=True`), so `record_gpu` would raise `ProgrammingError` inside a `finally` — silently skipping connection cleanup and dropping the ledger entry — on every watchdog-triggered teardown. The dry-run test had masked it (idle thresholds set to 9999). Restructured so the bill is *computed* at teardown (any thread, in-memory, accurate wall-seconds) but the DB *write* goes through `_flush_bill` on the entering thread only; added a `test_watchdog_idle_soft_bills_from_main_thread` regression test (fires the watchdog from its thread, asserts one `idle_soft_stop` ledger row > 0). Same restructure also fixed billing the SSH-handshake-retry window on a setup failure. A second review pass found the identical bug class at the call sites — a failing `POST /stop` (transient RunPod 5xx/timeout) raised before `_flush_bill` ran, so the bill was computed but never written — fixed by wrapping teardown/flush in `try/finally` (the stop exception still surfaces loudly, since a failed stop may mean a pod is still billing on RunPod's side); regression test `test_bill_written_even_when_stop_fails`. (5) Used **full request URLs** (`f"{RUNPOD_API}/pods"`) instead of httpx `base_url` — base_url + a leading-slash path silently drops the `/v1` segment (RFC 3986 join). REST shapes (`gpuTypeIds`, `cloudType` enum, `desiredStatus`, `portMappings`, `POST /pods/{id}/stop`) confirmed against the live `rest.runpod.io/v1/openapi.json`, not the card. **Not pod-verified this session** — no pod/key run here; the live test is `@pytest.mark.live` and skipped. Dry-run + full offline suite green (14 passed, 1 skipped, 1 deselected); same defer-to-real-pod posture as T10/T13.
**Verdict:** accepted (two scope adds explicitly approved by the user; the rest are gap-fills and one correctness fix, none touching parsing logic)
**Lesson:** A "GPU session" task is almost entirely lifecycle *correctness*, not parsing — the real bugs live in the teardown race, the bill-exactly-once guarantee, and "container RUNNING ≠ SSH-ready", so those must be designed explicitly; trusting the happy path would have leaked pods and double-billed.

## 2026-05-29 — T14 (live verification)
**What:** Ran the deferred live test on a real pod (per user). Several findings. (1) **Template ≠ pod:** the user first handed a *pod* ID (`4jgvnguwlf55a1`) as a "template"; `RunPodSession` creates its own pod *from a template*, so the create 500'd (`template not found`). Worse, a docling *pod* the user had started was left RUNNING at $0.86/hr (~$0.27 over 19 min) — surfaced it (didn't stop it myself, I hadn't created it); user terminated it. (2) **Parser images have no sshd:** the four palimpsest images can't be reached by `RunPodSession`'s direct TCP SSH (their template notes say so) — so T14's SSH design is incompatible with the parser images. **This is a T16 decision** (add sshd to the images, or proxy-exec + a network-volume file transfer); `scp_up`/`scp_down` are likewise untested against them. For T14 (whose live test is just `echo hello` → image-agnostic) I created a base sshd template (`runpod/pytorch:0.7.0-cu1241-torch241-ubuntu2204`, `PUBLIC_KEY` injected). (3) **Availability is dynamic — "Low" stock ≠ deployable:** every cheap GPU (4070Ti/4090 community, A5000/L4 secure, all "Low") returned `This machine does not have the resources to deploy your pod` (capacity 500, no pod, no charge); only H100/H200 ("High") actually deployed. Per user, made `gpu_type` accept `str | list[str]` → `gpuTypeIds` array → RunPod allocates whatever's in stock; the live test reads a cheapest-first `RUNPOD_GPU` list + `RUNPOD_CLOUD` from env (production default stays the single 4090/community). Ran on H100 with an env-raised cost cap (`RUNPOD_MAX_EUR`, default still the card's €0.10). (4) **Real bug only a live run could catch:** fabric's `Connection.run()` forwards stdin by default → `OSError: reading from stdin while output is captured` under pytest/CI/any non-interactive caller (and T16's parser runs are non-interactive) → fixed with `in_stream=False`. The first H100 run hit this and still tore the pod down cleanly (verified EXITED), proving the `__exit__`-on-exception teardown on real HW. (5) **Pass:** second run green — pod created → direct TCP SSH (account key injected) → `echo hello` → positive GPU ledger entry (< cap) → torn down. Terminated all test pods afterward (0 remaining). Spend ≈ $0.26 (two H100 runs; failed cheap-GPU creates were $0). Base template `klmsdkf0jd` kept for future live/T16 runs.
**Verdict:** accepted — live-verified; the stdin-forwarding bug and the dynamic-availability handling were only discoverable by running on real hardware, and the teardown held even on the failing run.
**Lesson:** RunPod "Low" stock is not a deployability promise — only an actual create tells you, so pass a GPU *list* and fall back across GPUs/clouds; and a green dry-run that mocks SSH can't catch transport bugs (stdin forwarding) — the live test paid for itself. (Open T16 input: parser images need an sshd story before `RunPodSession` can drive them, and `scp_up/scp_down` remain unproven.)

## 2026-05-29 — T17 (parser-set change + sshd in images)
**What:** Two changes to *locked* project rules, both user-directed, plus the resolution of the T14 sshd gap. (1) **Dropped olmOCR** — CLAUDE.md locked "docling, MinerU, olmOCR, Chandra" and the design's four-parser comparison, but olmOCR's only distribution is Allen AI's FP8/Ada-only prebuilt image (no Dockerfile we control, no sshd), which the user judged too painful for the self-built + SSH-able design. (2) **Expanded 4→5 parsers**, adding **dots.ocr** AND **PaddleOCR PP-StructureV3** (user: "more work upfront but better comparison"). The pick went through three rounds, each corrected by evidence: **Nougat** (user's first pick for diversity) → rejected after finding it's 2023/abandoned, CC-BY-NC, and the dead `nougat-ocr` pkg hard-pins `timm==0.5.4` (would repeat olmOCR's build pain; transformers-native Nougat avoids the pkg but the model is still stale) → **GOT-OCR2** (Apache, tiny, maintained) → rejected because it's end-to-end **text-only** and would blank 2 of 6 metrics (bbox precision, table-cell F1) → **dots.ocr + PaddleOCR**, both of which emit bbox + structured tables so all six metrics apply to all five. **Build facts corrected against live sources before writing Dockerfiles:** PaddleOCR must be **CUDA 12.6**, not the shared 12.8 — `curl`'d the Paddle wheel index and the cu128 index has NO py3.11 wheel while cu126 does (would have silently installed a CPU wheel, failing only on a paid pod); dots.ocr's own `parser.py` needs a vLLM server, so its baked `dots_run.py` runs the model **in-process via transformers** with **`sdpa`** (skipping the slow/fragile flash-attn build) and bakes weights to a **no-periods dir** (`/opt/weights/DotsOCR`, the trust_remote_code module-name workaround). (3) **sshd into all five images** (shared `docker/start.sh`) — this resolves the T14 entry's "parser images have no sshd → T16 decision": chose route (a) rebuild the images *with* sshd over route (b) proxy-exec + network volume, so `RunPodSession`'s direct TCP SSH+scp (and `scp_up/scp_down`) work unchanged. `$PUBLIC_KEY` is injected **per template** (not auto-injected by RunPod — the T14 base-template lesson). Methodology decided with the user: **per-parser scorecard now, parser-fusion deferred** (data-gated — don't build it speculatively; revisit only if the real corpus shows the extraction-winner and the bbox-winner are different parsers, since every triple needs a viewer-quality bbox). Authored offline; **build (free CI) + pod-verify deferred to night** (cheap GPUs).
**Verdict:** accepted — both locked-rule changes were explicit user decisions; the parser pick is evidence-driven (each candidate rejected on a concrete licence/metric/build fact); the cu126 catch and the sdpa/no-periods choices pre-empt paid-pod failures.
**Lesson:** For a *comparison* thesis the 4th/5th parser must be chosen on what it can *measure*, not just diversity — an end-to-end OCR model that emits no bbox/table-structure silently blanks two metrics, which only surfaces if you map each metric to a required parser capability first. And a vendor's "install" command (`pip install paddlepaddle-gpu`, dots.ocr's `parser.py`) hides the real constraints (which CUDA wheel index exists; whether it needs a vLLM server) — verify the wheel index and the inference path before baking, exactly as olmOCR's FP8 taught.

## 2026-05-29 — T17 (paddle CI build fix)
**What:** First CI build of the paddle image FAILED at the build-time guard `import paddle`:
`ImportError: libcuda.so.1: cannot open shared object file`. `paddlepaddle-gpu` hard-links the
NVIDIA driver lib (`libcuda.so.1`) at *import* — and the GitHub Actions builder has no GPU/driver
(the `nvidia/cuda:*-devel` base ships the CUDA toolkit, not the driver stub on the loader path).
PyTorch doesn't do this (it lazy-loads CUDA and tolerates no driver), which is why every torch
image's `import torch` assert passes in CI but `import paddle` cannot. Replaced the assert with a
no-import metadata check (`importlib.metadata.version('paddlepaddle-gpu')`, confirms the GPU wheel
is installed vs CPU `paddlepaddle`); the real `import paddle` + `is_compiled_with_cuda()` moves to
the pod (night). The same constraint kills the weight-bake (it predicts → imports paddle), so
PP-StructureV3 models now download on first pod run instead of baking — Paddle is the one parser
whose weights can't be pre-pulled at build (no GPU at build time).
**Verdict:** accepted (the import-time driver dependency is intrinsic to paddlepaddle-gpu; metadata is the right CI-time guard).
**Lesson:** Not every GPU framework defers the driver like PyTorch — `paddlepaddle-gpu` needs `libcuda.so.1` at *import*, so it can't be import-checked OR run (to bake weights) on a no-GPU CI builder. Verify GPU-framework wheels by metadata in CI; do the real import + weight bake on the GPU pod.

## 2026-05-29 — T15 (parser_runs CHECK uses T17 5-parser set, not card's 4)
**What:** T15 card line 20 froze the parser set at the 4-parser era:
`CHECK (parser_name IN ('docling','mineru','olmocr','chandra'))`,
and `has_all_parsers` is documented as "true iff **4 rows** exist … across all **4** parser names."
PROGRESS T17 dropped olmOCR (FP8/Ada-only vendor image, no sshd we control) and added dots.ocr +
PaddleOCR PP-StructureV3 → 5 parsers at image `0.2.0`. `cache.py` encodes the live set:
`PARSERS = ("docling", "mineru", "chandra", "dots", "paddle")`, the CHECK enforces the same five,
and `has_all_parsers` expects 5 distinct `parser_name` rows. Confirmed with user before writing.
Also clarified the storage shape the card under-specified: `output_path` is a relative string
under `cache_dir` and may point to a **file OR a directory** — 3 of the 5 parsers emit a dir
(MinerU/Chandra/Paddle: markdown + images + structured JSON); docling and dots.ocr emit a single
JSON. T16 owns the per-parser convention; T15 is content-agnostic.
**Verdict:** accepted (T17 supersedes T15's frozen set; the cache shape is unchanged by the pivot
since the key is per-parser-independent — only the set membership moved).
**Lesson:** When a downstream task (T15) lists a constituency (the 4 parser names), check the
latest upstream re-issue (T17) before encoding it — a card that froze "the four parsers" doesn't
self-update when "the four parsers" stops being the canonical set.

## 2026-06-08 — T23
**What:** The card's failing-test example ("Overpotential missing `condition` → SHACL fails, report mentions the missing slot") is wrong against the *currently generated* `schema/generated/shacl.ttl`. LinkML's `gen-shacl` emits `sh:minCount` only for slots marked `required: true` in the YAML; `Measurement.condition` isn't (only 5 slots are: `Paper.sha256` + `Evidence.{paper, page, bbox, parser_name}` from T19's audit). The generator produced `sh:maxCount 1` but no `sh:minCount` on the condition predicate, so a missing-condition Overpotential passes SHACL. User-approved (via AskUserQuestion pre-implementation) to pivot the failing test to `Evidence` missing `parser_name` — a real `sh:minCount 1` catch — rather than patch the schema to make the card's literal example true.
**Verdict:** accepted (pivoted test, schema unchanged)
**Lesson:** SHACL constraints come from `required: true` in the LinkML YAML, not from prose examples in task cards. Always verify a SHACL-side test premise against the actually-generated `shacl.ttl`, not the card's narrative.

## 2026-06-08 — T23
**What:** First reviewer pass on T23 (`acc5279`) added `test_batch_returns_failure_in_place` to address a "batch with failure" test gap. Second reviewer pass (post-commit, on the as-merged state) correctly diagnosed the test as misnamed and redundant — it never actually called `validate_batch`, just duplicated `test_evidence_missing_parser_name_fails`'s `_validate_jsonld` path. A meaningful `validate_batch` mixed-pass/fail test requires a Pydantic-valid + SHACL-fail input, which doesn't exist in the current schema (every `sh:minCount 1` slot is also Pydantic-required, so any SHACL-fail item is rejected by Pydantic at construction). Deleted the test in `1b97f78`; replaced with a code comment naming the gap and the conditions under which it could be honestly filled (F4 lands or T24 introduces an external JSON-LD path).
**Verdict:** accepted — admitting the test gap is more honest than testing under a hypothetical that doesn't exist; the wrapper itself is a trivially-correct one-line comprehension.
**Lesson:** A test that exercises private internals while claiming to verify the public surface is worse than no test at all — it fakes coverage. If the only way to construct an interesting failing input is to write the test against a known bug, defer the test.

## 2026-06-08 — T18a (F4)
**What:** Three fix paths existed for the T23-reviewer-surfaced bbox-dedup bug (`bbox: list[float]` with `sh:minCount 4 ; sh:maxCount 4` on a single repeated predicate fails because RDF dedups identical literals on the same subject+predicate, so any axis-aligned region or single-line text span with two equal coordinates trips `sh:minCount 4`): (a) model `bbox` as an `rdf:List` (LinkML's `inlined_as_list` — investigation-unknown overhead, potentially needs a custom shape post-processor); (b) split into 4 typed per-corner predicates `bbox_x0`/`bbox_y0`/`bbox_x1`/`bbox_y1` (verbose schema, contained cascade); (c) drop SHACL cardinality, rely on Pydantic's `min_length=4, max_length=4` (cheapest, but SHACL no longer authoritative on bbox). User-approved (b) over the reviewer's tentative ranking. Implemented in commit `5233d45`; degenerate `[0,0,1,1]` (was the dedup-fatal case) now passes `validate_instance`.
**Verdict:** accepted (option b is schema-correct, no LinkML internals investigation needed, cascade was ~15 LOC across 5 files)
**Lesson:** SHACL `sh:minCount N` on a `multivalued: true` slot expressed via a single repeated predicate is a footgun whenever element equality is plausible — RDF's set semantics will collapse equal literals. For tuple-shaped data (bbox, RGB, anything with a fixed positional contract), use typed per-position predicates from the start rather than relying on cardinality of a repeated edge.

## 2026-06-08 — T24
**What:** Card prescribed `insert_extraction(self, instance, paper_sha, page, bbox: tuple[float,float,float,float], parser_name, run_id, source_text="")` with 6 explicit provenance positional args. After T19's audit + T18a F4's bbox split, all 7 provenance fields are Pydantic-required on `Evidence` (a Measurement that reaches T23/T24 has them; T22's `_inject_provenance` further ensures the caller-owned ones are correct). Re-passing them duplicates work T22 already did. Slimmed the signature to `insert_extraction(instance, *, run_id, parse_run_id=None) -> str`: Evidence pulled from `instance.evidence`, `run_id` stays explicit because it's not a schema field (CostMeter ledger concern per card line 16), `parse_run_id` is the card's explicit "decide" option. Documented in the module docstring; T24 card itself was NOT amended.
**Verdict:** accepted (the slim signature is what T25's caller will actually use; the verbose signature would force the caller to unpack `instance.evidence` for every insert)
**Lesson:** Once provenance carriers (Evidence) are made required at the validated boundary, downstream signatures should trust that boundary instead of duplicating the args. The card pre-dated T19's `required: true` audit and T18a's F4 split — both made the original signature redundant.

## 2026-06-08 — T18a (F2 cascade miss)
**What:** F2 removed 5 orphan slots (`overpotential_at_10mAcm2`, `activation_overpotential`, `anodic_overpotential`, `catalyst`, `oer_reaction`) from `schema/palimpsest.yaml` in commit `5233d45`. The cascade audit pre-edit ran `grep -rn "bbox\|<slot>" src/ tests/ skills/ schema/palimpsest.yaml docker/...` — that covered schema, generated artifacts, tests, and `extract.py`'s system-prompt string. It missed `skills/oer-extraction/SKILL.md` row 23, which still taught the LLM to emit `value` under one of the three deleted typed-overpotential slot names. SKILL.md is production prompt content loaded via `read_skill` at every extraction; an LLM following that row would have emitted JSON Pydantic rejects (`extra="forbid"`) with a confusing "Extra inputs are not permitted" error. Caught by the post-commit reviewer pass (B1, blocking); fixed in `f896a40` by rewriting row 23 to point at `Overpotential` class with `value` + `unit_label` + `condition.current_density` disambiguation, matching every other row in the table's shape.
**Verdict:** accepted (real production bug found by reviewer; F2 was correct to delete the slots, the SKILL.md row was just stale)
**Lesson:** Schema-cleanup cascade audits should grep prose-content files (SKILL.md, READMEs, task cards) explicitly, not just code/tests. The audit's mental model was "find the slot name in code-shaped places"; the missed file is what the *LLM* reads, not what *Python* imports — exactly where stale schema references silently mislead downstream callers.

## 2026-06-08 — process (reviewer cadence)
**What:** Session ran 4 substantive commits (`acc5279` T23, `1b97f78` T23 polish, `5233d45` T18a, `5193e3a` T24). CLAUDE.md global §3 ("Review should be independent from implementation") and the per-task workflow argue for an independent reviewer dispatch per substantive commit — strictly, 4 dispatches over this session. Actual cadence: one reviewer dispatch over the T23 initial commit (pre-merge, flagged 4 gaps), a second reviewer dispatch over the T23 polished commit (post-merge, caught the misnamed-test issue and reframed F4 as a T24 blocker), and one reviewer dispatch over the combined T18a + T24 commits (post-merge, found the B1 SKILL.md cascade miss). User explicitly requested "launch a reviewer agent" (singular) for the T18a + T24 batch.
**Verdict:** accepted partially — honored the user's explicit cadence request, but flagging the global-rule deviation here so the thesis reflection has it.
**Lesson:** Batching review across commits costs you per-commit per-reviewer specificity; the reviewer found B1 only because the T18a commit's prose-file cascade was inside the same review window as T24, so the gap was visible against the surrounding work. A per-commit review would have caught it earlier (smaller diff, narrower attention), but the per-batch review found it eventually. The cost was one extra commit (`f896a40`); the dollar-cost trade vs. four separate reviewer invocations is roughly even for this size of work.

## 2026-06-11 — T48 (card premise + verify command stale)
**What:** Two card facts were stale on arrival, neither a code fix (so distinct from the hardware corrections this card anticipated logging here). (1) The card's central premise — "the pipeline has **never run on real GPU hardware**" — was already false: the T16/T17 live runs parsed all 5 parsers on canonical `0.2.2` images on SECURE RunPod GPUs (2026-05-31), leaving 5 `parser_runs` rows with real `gpu_cost_eur` + 5 cached outputs for `sha=3432d049…`. (2) The card's verification command `pixi run python -m palimpsest parse papers/<sample>.pdf` references a `parse` subcommand that does not exist — `__main__.py` is the T08 one-shot agent loop; `parse_with_cache` is a library function with no CLI wrapper. Per user, T48 was scoped to **document-only** (no new GPU spend): consolidated the existing, already-passing hardware evidence into the card's designated `notes/parser-hardware-verify.md` rather than re-running paid pods to re-prove a met bar.
**Verdict:** accepted — re-running 5 paid pods to reproduce evidence the ledger + cache already hold would burn budget for no new information; the honest deliverable is the write-up. No `src/`, schema, or test touched; DEVIATIONS gets this card-fact note only (no new hardware correction was found).
**Lesson:** A task card written before its dependencies completed can assert a premise its own dependencies already invalidated. Check the actual end-state (DB rows, cache files) against the card's "input/why" before assuming the work is unstarted — the card's framing is a snapshot from when it was written, not ground truth at execution time.

## 2026-06-15 — T26 (CostMeter check_same_thread + sealed-module edit)
**What:** Two card-vs-reality deviations. (1) The T26 card's "Notes" suggest a Textual `@work` worker for responsiveness, but `agent.run()` (in that worker) writes to `CostMeter`, whose sqlite connection was opened with the default `check_same_thread=True` — accessing it from the worker thread raises `ProgrammingError`. A stub-agent smoke test never touches sqlite, so the crash would have shipped green. Edited `cost.py` (sealed since T05) to `sqlite3.connect(db_path, check_same_thread=False)` + an invariant comment. The change is strictly more permissive (no API/behavior change for the existing single-threaded callers; full suite green). Serialization is provided **by construction**, not by `exclusive=True`: cancelling a Textual *thread* worker does not kill the OS thread (Python can't), so a still-running call could write concurrently — instead the `Input` is disabled while a request is in flight (single-in-flight), and the cost bar is refreshed via `call_from_thread` *after* the worker returns (a sync barrier). So no `threading.Lock`; the comment names the future trigger (a timer-based live cost bar) that would require one. (2) Card line 16 says `main()` constructs `AnthropicProvider` + `ParserCache` + `RDFStore` + `SkillLoader`; runtime default is DeepSeek (T50) and the `Agent` takes none of those last three (the tools build their own deps), so `main()` wires `DeepSeekProvider` only — constructing the unused three would be speculative (CLAUDE.md §2).
**Verdict:** accepted — user explicitly authorized modifying sealed code for the right design; advisor-confirmed Option B over a synchronous freeze (poor for a chat UI's core action) and over a bespoke thread+queue (more surface than one permissive line). Precedent for editing a "sealed" module when justified: T19 patched T18's schema, T50 touched the schema.
**Lesson:** `check_same_thread=True` is a *correctness tripwire that a stub test silently passes* — the moment a sealed sync component is pulled into a threaded UI, audit its thread-affinity at the boundary, not via the happy-path test. And `exclusive=True` on a Textual thread worker is not a serialization primitive (it cancels the await, not the thread); serialize the *input source* instead.

## 2026-06-15 — T27 (4th file beyond the card's 3)
**What:** The card's "Will touch" lists 3 files (`tui/slash.py` new, `tui/app.py` edit, `tests/test_slash.py` new). The implementation also edited a 4th: `tests/test_tui.py`. T26 had left `test_slash_command_stubbed`, which pinned the T26 placeholder behavior (`/`-input writes "slash commands land in T27", agent not called). T27 replaces that stub with a real `dispatch()` call, so the test's `assert "T27" in log_text` necessarily breaks. Renamed/rewrote it to `test_slash_command_dispatched` (asserts the unknown-command message for the now-deferred `/budget`, agent still not called), and added two in-app pilot tests — `test_slash_help_lists_commands_in_app` and `test_slash_quit_exits_the_app` — to cover the card's manual `pixi run tui` step (`/help` lists, `/quit` exits) headlessly. The `/quit` test asserts `app._exit is True` + the post-exit `"bye"` write doesn't crash (the real integration risk an advisor flagged).
**Verdict:** accepted — editing `test_tui.py` is unavoidable: T27 intentionally changes the behavior that test pinned, so leaving it would ship a red suite. Cleaning up a test made stale by my own change is CLAUDE.md §3 ("clean up your own mess"), not scope creep. The two added pilot tests close the only gap independent review + advisor surfaced (the card's manual step had no automated coverage).
**Lesson:** A placeholder test that pins "land in T<next>" stub text is a known tripwire for the task that fills it — expect to rewrite it, and budget the follow-on file in the card's "Will touch" when authoring forward-referencing stubs.

## 2026-06-15 — T28 (/model wiring supersedes pre-T50 card text; 2 retargeted tests)
**What:** Two deviations. (1) The T28 card (lines 14-16) says "only `sonnet` (already implemented in T04) is wired; `/model haiku|deepseek|gemini` returns 'provider X not implemented yet; only sonnet is available in MVP'." That text predates T50, which made `DeepSeekProvider` the runtime default and `AnthropicProvider` the kept fallback — both are real, importable classes. Per a user decision this session (AskUserQuestion), `/model` now wires **both** real providers: `/model sonnet` → `AnthropicProvider`, `/model deepseek` → `DeepSeekProvider` (each reconstructs and reassigns `app.agent.provider`, returns `"switched to {name} (prompt cache reset)"`); `/model haiku|gemini` → `"provider 'X' not implemented yet"`; unknown/missing → `"unknown model: X. options: sonnet, deepseek"`. Provider construction is wrapped in try/except so a missing API key surfaces as `"could not switch to X: …"` rather than crashing the TUI. (2) Two pre-existing tests used `/budget` as their "unknown command" probe (`tests/test_slash.py::test_unknown_command_returns_help_hint` and `tests/test_tui.py::test_slash_command_dispatched`, the latter rewritten in T27 to pin `/budget` as deferred). Implementing `/budget` necessarily makes those assertions false, so both were retargeted to `/parser` (out of scope per the T28 card), preserving each test's intent (slash routed to the dispatcher, agent never called, unknown handled gracefully). `tests/test_tui.py` is thus a 3rd/4th touched file beyond the card's stated 2 — same unavoidable-stale-test situation as T27.
**Verdict:** accepted — the card's sonnet-only framing is a snapshot from before T50; wiring the actual runtime default (DeepSeek) into `/model` is the honest behavior, and the user chose it explicitly. `cost.py` and `agent.py` (card's "Will NOT touch") are genuinely untouched: `/cost` reads the ledger via the already-public `app.cost_meter.conn` rather than adding an aggregation method to the sealed `cost.py`. Independent reviewer verdict PASS_WITH_CAVEATS, 0 blocking.
**Lesson:** A card written before a stack-changing task (T50 flipped the default LLM) can prescribe behavior that contradicts the live system; reconcile `/model`-style "switch the thing" commands against what's actually running, not the card's snapshot. And exposing a sealed component's *connection* as a read surface lets a consumer build a new view (`/cost`) without editing the off-limits module.

## 2026-06-15 — T29 (PDF location by content-hash; 2 PDF.js files not ~10; +1 test file)
**What:** Three deviations. (1) The card assumes `/paper/{sha}/pdf` can stream the PDF located via the `papers` table, but that table stores `filename = "sample.pdf"` (a T07 fixture artifact) while `papers/` on disk holds DOI-named files and has no `sample.pdf` — the DB filename cannot locate the bytes. Resolved by building a `sha256 -> Path` index that hashes every `papers/*.pdf` via T07 `read_paper` (`@lru_cache`d); the route serves the hash-matched file, so the bytes served for `{sha}` hash to exactly `{sha}` (provenance-true), and `cache.py` stays untouched. `GET /paper/{sha}` and `/paper/{sha}/pdf` 404 when no file hashes to `{sha}`. (2) The card says vendor "~10 files" of PDF.js (`pdf.min.mjs`, `pdf.worker.min.mjs`, *viewer assets*). We render manually into our own Jinja2 template (custom canvas loop), so PDF.js's prebuilt viewer UI is never used — only the 2 core ESM files are vendored (pinned pdf.js 4.10.38 from cdnjs, + a `VERSION.txt`). Verified in a real browser: all 12 pages render to 12 canvases. (3) Added `tests/test_viewer.py` (offline FastAPI `TestClient`: `/health`, `/`, 404s, and a self-skipping real-PDF stream test) — a test file beyond the card's 4 "Will touch" entries, consistent with the per-task test discipline.
**Verdict:** accepted — (1) is the honest reconciliation of a card written against an assumed filename convention that the actual DB state contradicts; content-addressing is strictly more correct than trusting `filename` and needs no schema/cache change. (2) vendoring only what the code imports is CLAUDE.md §2 (minimum, nothing speculative) — the viewer-asset files would be dead weight. (3) matches T27/T28 precedent. Also: HTMX loaded from the CDN per the card (line 18), not vendored, despite CLAUDE.md's stack line listing "vendored HTMX" — the card is the task-level source of truth and HTMX isn't exercised until T30; revisit vendoring then if the CDN dep is unwanted.
**Lesson:** When a card's data-access assumption (locate file by DB `filename`) collides with reality (fixture filename + content-addressed store), prefer the store's own invariant (SHA-256 of bytes) over the convenience column — it both fixes the bug and reinforces the provenance guarantee for free. And "vendor the library" means vendor what you import; a manual render path doesn't need the library's bundled UI.

### T29 follow-on (2026-06-16) — cmaps + standard_fonts vendored after all
Deviation #2 above ("only 2 ESM files vendored") was revisited after a user design
question about whether the slim viewer is "good enough as a product." The prebuilt
PDF.js **viewer UI** stays un-vendored (still dead weight — we render manually and
T31's bbox overlay needs that control), but `cmaps/` (169 files) + `standard_fonts/`
(16 files) WERE added from the same pinned 4.10.38 dist zip and wired via
`cMapUrl`/`cMapPacked`/`standardFontDataUrl`. These are **rendering data, not UI**:
PDF.js needs them for CID/CJK-encoded PDFs and PDFs that don't embed their fonts.
Without them a real-corpus PDF could render with missing/wrong glyphs (the demo paper
dodged it by being Latin-script with embedded fonts). Re-verified: 12 pages render,
0 console warnings. **Refined rule:** vendor what you *import* AND the data your
import needs to render correctly — but not the bundled UI you replaced.

## 2026-06-17 — T32 (two accepted MVP limitations, deferred for later)
**What:** `open_notebook` shipped with two known gaps that both independent reviewers flagged as non-blocking; per a user decision we leave them for now and record them here so they aren't lost. (1) **No user-facing keep-alive/close control.** Spawned marimo processes are tracked in a module-level `_PROCESSES` list (`tools/open_notebook.py`) but never reaped — they outlive the agent turn and rely on the user closing the browser / Ctrl+C, exactly as the card sanctioned ("just leak"). We considered (a) an `atexit` reaper and (b) a `keep_alive` tool arg / TUI toggle, and rejected both for now: a tool arg is set by the *LLM*, not the human, so it isn't real user control; killing a marimo editor when the agent exits is arguably the *wrong* default (the human is mid-edit in the browser); and a configurable toggle is the kind of unrequested flexibility CLAUDE.md §2 refuses. The leave-alive default is the free, usually-correct behavior. (2) **The stdout read has no timeout.** `for line in proc.stdout` returns on the URL line and raises `RuntimeError` on EOF (process died), but if marimo *starts and never prints a matching `http://localhost:<port>` line* the loop blocks indefinitely and wedges the agent turn — no deadline. A ~5-LOC `threading.Timer(proc.terminate)` watchdog would close this (timer fires → stdout EOFs → existing RuntimeError path), testable offline with a blocking fake + a short monkeypatched timeout. Deferred, not built, this session.
**Verdict:** partially/accepted-as-MVP — both are genuine limitations, neither blocks T32's carded scope. (1) is a deliberate product choice (keep-alive default) with any real human-facing close control deferred to a future TUI shutdown-prompt task, where it would be an actual user decision rather than an LLM-set flag. (2) is a real reliability gap (a tool that can hang the agent) but does not occur on marimo's happy path (live-verified: 0.23.8 prints the localhost URL on headless start); fix is low-risk and small when we return to it.
**Lesson:** "Let the user decide" inside an agent is not the same as "add a tool parameter" — a parameter the LLM fills is the *model* guessing intent, so genuine user control belongs at the human-facing layer (TUI), not the tool signature. And a blocking `for line in proc.stdout` with only an EOF exit is a latent agent-hang: pair every subprocess-stdout scrape with a deadline, or document why you didn't.

## 2026-06-17 — T34/T35 (scaled to local 5-paper corpus; chandra dropped; GT from parser-consensus)
**What:** Three deviations from the T34/T35 cards, all user-approved.
(1) **No 25-paper corpus.** `papers/` holds 5 PDFs (1 pre-cached + 4 new), not 25. Per user, parsed the 4 local papers now and deferred the 25-paper run until the parser comparison validates which parsers to keep (cost). T34's literal `assert len(incomplete)==0` therefore reports 4 incomplete (each missing only chandra); reframed as "data" per the card's own "some parsers will fail; that's data" note. Also: T34 verify snippet #2 calls `CostMeter.list_ledger()`, which does not exist — the real surface is `total_eur()` only, so GPU spend was queried via raw SQL `WHERE kind='gpu'` (same approach as T28 `/cost`).
(2) **chandra dropped (0/4 on the new papers).** Two full attempts both failed identically: chandra's parse command hits the runner's hardcoded per-paper `ssh(timeout=2400)` = 40 min on these figure-dense Nature papers (its ~5B VLM transcribes every TEM/XRD/spectra panel — ~18 min on the 12-page sample but >40 min here), the timeout kills the command ("bad command exit code"), and the pod's SSH transport dies, taking the remaining queued papers with it. First attempt (alphabetical order) crashed on the heaviest paper; second (lightest-first + 48GB-GPU preference, which fell through to a 3090 as the big cards were capacity-500) crashed on the lightest — confirming it is chandra-wide-on-this-corpus, not paper-specific OOM. Could not extend the timeout without editing `runner.py` (T16 do-not-touch). chandra has no bbox geometry → already excluded from extraction (T49) → dropping it costs the graph nothing; only the T36 text-accuracy metric loses one parser column. Final corpus = 4 parsers (docling/mineru/dots/paddle) × 5 papers.
(3) **GT methodology (T35).** GT built from **4-parser consensus** (a per-paper drafting agent reading the parses) + **independent adversarial reviewer agents** that re-derive from the parses — NOT a single LLM PDF reading. This is the user's design to keep GT *grounded* in extractable content. Residual caveat: GT derived from parser output is not fully independent of the parsers it later scores (T36–T39) — a value no parser captured cannot enter GT; mitigated by 4-parser consensus + a retained independent PDF-read anchor (`experiments/ground_truth_<stem>.pdfread.md`) per paper. GT files are marked grounded-but-recommend-human-glance, not human-verified.
**Verdict:** accepted. The 5-paper / 4-parser corpus + grounded, reviewer-checked GT is a valid testing basis; the 25-paper run is a future cost-gated step.
**Lesson:** a parser's wall-clock budget is content-dependent, not page-count-dependent — a figure-dense paper can 2–3× a heavy VLM's time and blow a fixed SSH timeout, and a hard-crashing parse can take the whole pod (and its queued work) down, so order work lightest-first to bound the blast radius. And multi-parser consensus + adversarial review beats a single reader: it corrected 4 values in the first-pass PDF-read GT drafts.

## 2026-06-18 — agentic layer (constrained-autonomy agent)
**What:** Pivoted palimpsest from an extraction-only ~400-LOC tool to a constrained-autonomy agent (now the thesis core, per user): added general read/write/edit/bash + sparql/extract_paper tools, an in-code workspace policy, dulwich versioning, and config bootstrap. Two seams were left deliberately unresolved: (1) **dev split-brain** — `extract_paper`/`CostMeter`/parser-`cache` resolve cwd-relative (repo `store/`, `palimpsest.db`, `cache/`) while the agent's *file* workspace is `./workspace`; (2) the workspace-root default is a `./workspace` subfolder (a dev-safety choice so the agent can't touch the engine repo), which is NOT the deployment model (spawn folder = the workspace). Also: `bash` is intentionally a cwd-pinned *supervised escape hatch*, not filesystem-fenced (the Claude Code model) — mitigated by the dulwich audit/undo net rather than an OS sandbox.
**Verdict:** accepted (deferred to a later portability phase).
**Lesson:** keep "where state lives" out of the engine. The functions are already parameterized (`run_paper`/`extract` take injectable `store`/`cache`/`cost_meter`), so unifying paths and choosing per-workspace (graph) vs global (cache, €-ledger) is a deployment-layer wiring step — not an engine rewrite. Build in the factory now; resolve location + packaging when portability is tackled.

## 2026-06-18 — T57 (scope expansion: persisted run log for a real dropped count)
**What:** The T57 card scoped `workspace_status` as "€0 pure read" reusing only `cache.list_all_papers` / `RDFStore.sparql` / `read_paper`, with "Will touch" = the new tool + `tools/__init__.py` + its test. Building it surfaced a contradiction the card couldn't satisfy: a **true** per-paper *dropped count* is not derivable from those three read-only sources. Drops = `extract()`-level `errors` (mis-citation/unit/missing-evidence/unknown-class/Pydantic) **plus** SHACL drops **plus** insert refusals — all computed at run time and then **discarded** (`pipeline.run_paper` did `valid, _errors = extract(...)` and threw `_errors` away; there was no extraction-runs record anywhere). So the only card-consistent options were (a) fabricate a structural proxy, (b) re-run the LLM on every status call (not €0, non-deterministic), or (c) persist the counts a run already computes and read them. Per explicit user direction — *"find the actual solve and industry best practice; do not limit yourself to the card"* — chose (c), the standard observability pattern (CI run-results / dbt `run_results.json` / MLflow runs: a status view reads persisted run metadata, it does not recompute). Added `src/palimpsest/runs.py` (`ExtractionRunLog`: an `extraction_runs` table in `palimpsest.db`, mirroring `parser_runs` — same SQLite-ledger architecture, `latest_per_paper()` by max rowid) + one `run_log.record(...)` call at the end of `pipeline.run_paper` capturing `n_errors`/`n_extracted`/`n_validated`/`n_inserted`. `run_paper`'s **return dict is unchanged** (recording is a pure side effect); `run_log` is an injectable kwarg (default `ExtractionRunLog("palimpsest.db")`), and the offline pipeline smoke test was updated to inject a tmp run_log so it stays hermetic. `workspace_status` reports `dropped = (n_extracted + n_errors) − n_inserted` from the latest recorded run; a paper with graph measurements but no recorded run (e.g. a pre-T57 demo insert) is reported honestly with "(no run metadata — drop count unavailable, see T58)" rather than a fabricated number. Independent reviewer: PASS_WITH_CAVEATS, 0 blocking; verified the dropped math, the `_GRAPH_COUNTS` SPARQL predicates against `store.py`, and that no code-enforced invariant (workspace confinement / provenance-on-insert / €-budget gate) is weakened. Applied its one literal-inaccuracy fix (the "costs nothing / pure read" wording now states no graph/ledger/data writes but a `CREATE TABLE IF NOT EXISTS` on construct, same DDL-on-construct as `ParserCache`/`CostMeter`) + its top test-gap (registered-but-unparsed branch). Verification: `ANTHROPIC_API_KEY="" pixi run pytest -m "not slow"` → 208 passed / 8 skipped / 25 deselected, 0 failures; tool smoke-rendered against the real repo cache + on-disk graph (5 papers, matches T34's 4/5-parse + chandra-dropped + sample.pdf's 6 graph measurements).
**Verdict:** accepted (user-authorized). The expansion adds observability, weakens nothing, and is the persistence layer **T58** (drop *reasons*) now reads instead of re-instrumenting `run_paper`.
**Lesson:** when a card asks for output its own hard constraints can't produce (here: a dropped count from read-only sources that never persisted drops), the fix isn't to satisfy the wording with a guess — it's to name the missing capability (persisted run metadata) and close it the standard way. A "status" command reads recorded state; it never recomputes expensive derivations on demand. Doing it once also turned T58 from a re-instrumentation into a thin reader.

## 2026-06-18 — T65 (scope add: test_tui.py; design: caller-owns-clearing to avoid a cross-thread race)
**What:** Two notes. (1) **Scope add.** The T65 card's "Will touch" lists `tests/test_agent.py` as the only test file, but the feature has a TUI half (Esc → cancel) that needs coverage, so `tests/test_tui.py` gained 2 pilot tests (Esc-in-flight sets the shared `threading.Event` + idle-Esc no-op; submit clears a stale cancel). Same unavoidable situation as T26/T27/T63, where TUI behavior is tested in `test_tui.py`. The 3 *source* files the card names (`agent.py`, `tui/app.py`, `tests/test_agent.py`) are otherwise exactly matched; CLI + `build_agent` untouched. (2) **Design — the caller owns the cancel flag's lifecycle.** The natural first cut had `Agent.run()` clear the event at entry (defensive: a stale set from a prior run can't abort a fresh one). The first independent review caught that this clear runs on the **worker thread** while the TUI sets the flag on the **main thread** (Esc), opening a real race: an Esc pressed in turn-1's worker-startup window (after `input.disabled=True`, before the worker's `clear()`) is silently wiped and the user must press Esc again. Fix: the agent no longer auto-clears (contract: *the caller clears before each run*); the TUI clears on the main thread in `on_input_submitted`, synchronously before arming the in-flight guard, where Textual's single-threaded cooperative event loop guarantees no Esc keybinding can interleave. A fresh independent reviewer (no prior context, re-ran the suite) then flagged that my code comment still justified the clear by a main-thread "interleave race" that cannot occur — reworded to the true rationale (wiping a stale set from a late Esc fired as the previous turn ended).
**Verdict:** accepted. Both are improvements surfaced by independent review; neither weakens a code-enforced invariant — the cancel check precedes `check_or_raise`, so it only ever *prevents* spend (€-budget gate intact), and nothing is appended to `self.messages` on the cancel path, so the cached system+tools prefix is untouched.
**Lesson:** a "defensive" clear/reset is only defensive if it runs on the thread that owns the signal. Clearing shared cross-thread state from the *consumer* (the worker) races the *producer* (the main-thread Esc); put lifecycle ops (clear-before-run) on the producer's thread and let the consumer only *read*. And when you keep a guard, make its comment state the reason it actually defends — a plausible-but-wrong rationale is a maintenance trap an independent pair of eyes will (rightly) flag.

## 2026-06-18 — T66 (scope expansion: secret redaction + gitignore the transcript; +policy.py/versioning.py beyond the card's 3 files)
**What:** The card's "Will touch" named `agent.py` + a session helper + `tests/`. The first cut matched that exactly (PASS_WITH_CAVEATS), but on the user's instruction to fix the surfaced caveats rather than defer them, two changes reached two more source files — flagged here. (1) **Secret redaction (`policy.py` + `agent.py`).** Reads are unconditionally allowed by design, so a `read_file(".env")` result would persist into the transcript. Rather than refuse secret reads (which would change the documented "reads are safe" contract and wouldn't stop `bash cat .env`), redaction lives at the **log layer**: `policy.is_secret_path()` (reusing `_PROTECTED_NAMES` ∪ `*.key`) + `Agent._redact_secrets()` masks the secret `tool_result` block in a *copy* before logging, leaving `self.messages` real so the live session still functions. (2) **Gitignore the transcript (`versioning.py`).** The first cut left `.palimpsest/` tracked, which an independent review showed put the append-only record inside `/undo`'s hard-reset tree — so `/undo` would truncate the reflection record (data loss). The user chose to gitignore `.palimpsest/`: durable on disk (reload reads the file, not git), immune to `/undo`, and a defence-in-depth backstop for the redaction (a leaked secret never reaches a commit). The `ensure_repo` upgrade branch was generalized to backfill the full required-ignore *set* (so a hand-edited gitignore missing `.env` is still repaired). (3) Minor: `load()` tolerates a truncated final line (the crash-then-resume case append non-atomicity creates). **Reload-on-spawn stays deferred** (card: "optional") — `load()` ships + is tested, but wiring `build_agent` to auto-resume is a per-spawn cost/cache change the human should opt into, not a silent default.
**Verdict:** accepted (user-directed). The card's 3-file scope is exceeded by `policy.py` (one pure helper) and `versioning.py` (one ignore line + a backfill generalization), both the minimal site for the fix; no code-enforced invariant is weakened (prompt-cache prefix untouched — redaction is on a logged *copy*, never `self.messages`; €0; workspace-confined; provenance not in this path), and the secret/undo posture is *strengthened*. Three independent reviewer passes; the redaction was adversarially attacked in the final pass and held for the structured tools.
**Known accepted limits (documented in code, not fixed):** a non-secret-named **symlink** to `.env` evades basename matching (closing it needs a filesystem `realpath` we avoid; gitignore is the backstop); `bash` output is unredacted (the supervised escape hatch — same human-responsibility model as its spend); a future content-returning tool keyed off something other than `input["path"]` would bypass redaction.
**Lesson:** a new persistence surface inherits the secret/rollback posture of everything that flows into it — the moment ephemeral in-memory tool output becomes a durable, version-controlled file, "reads are safe" and "the agent can undo anything" stop being free and have to be re-earned (redact at the persistence boundary; keep the append-only record out of the undoable tree). And a card's file list is a floor, not a ceiling: when fixing the caveat needs the helper to live in `policy.py`, put it there and log the expansion rather than contorting to stay inside three files.

## 2026-06-19 — T62 (`describe_schema`/`graph_summary`: catalyst gap, predicates-not-slot-names, SETUP not bumped)
**What:** Three notes on the read-side autonomy tools. (1) **Catalyst count is un-derivable, so it's reported as such, not fabricated.** The T62 card lists `#catalysts` for `graph_summary`, but `store.insert_extraction` never persists a catalyst node — it writes only measurement / evidence / paper / condition / electrolyte (verified by reading `store.py`, confirmed by the reviewer). There is no catalyst edge to COUNT. Rather than invent a proxy, `graph_summary` prints "catalysts: not persisted to the graph (store.py inserts no catalyst node; see DEVIATIONS 2026-06-19)". A real fix (a catalyst entity in the schema + store insert) is out of T62 scope. (2) **`describe_schema` surfaces RDF predicates, not Python slot names — beyond the card's literal "slots", required by the card's own Why.** The first cut listed `cls.model_fields` (`value`, `unit_label`, `electrode_potential_vs_rhe`, …) and class CURIEs. Independent review correctly flagged that the card's stated purpose — "build correct SPARQL … read the schema contract (classes/slots/units/**IRIs**)" — is not served by Python field names: the graph predicates are `palimpsest:value`, `palimpsest:unitLabel`, `palimpsest:potentialVsRHE`, and `electrolyte_ph` lives on the Electrolyte sub-node (`?c palimpsest:electrolyte ?e . ?e palimpsest:electrolytePH`), so an agent following slot names writes zero-row queries. Rewrote `describe_schema` to emit a SPARQL prefix block, measurement type IRIs, and the Condition/Electrolyte predicates **driven by iterating `RDFStore._COND_SCALARS` / `_ELECTROLYTE_SCALARS` + `store._PREFIX`** — the single source of truth, so the CURIE shown is byte-for-byte what's inserted (re-verified in sync by the reviewer). This reuses private class attrs deliberately: re-listing predicates by hand was the original defect, and there is no public schema-description surface to use instead. Importing `store` inside `describe_schema` (previously pyoxigraph-free) is acceptable — it reads class attrs only, opens no graph. (3) **SETUP.md doc-pointer NOT bumped.** The per-task convention bumps SETUP.md line 60 to the next task, but (a) the file was already `M` before this session (same situation T31 documented and left alone), and (b) the "next task" is genuinely ambiguous — T60/T61 are still undone and the card pairs T62 with T69/T70. Picking one would be guessing the supervisor's sequencing, so the pointer is left as-is.
**Verdict:** accepted. (1) is honesty over fabrication (CLAUDE.md + the "verify against actual schema" rule); (2) is a review-driven correctness fix that makes the autonomy-critical half actually usable, within the card's own rationale; (3) avoids a guess on a file already dirty from outside this task. No code-enforced invariant touched — both tools are read-only, €0, never in the provider call path; `graph_summary` opens the engine `store/` read-only and degrades on a RocksDB lock. Two independent reviewer passes (PASS_WITH_CAVEATS → all non-blockers applied → PASS, 0 blocking).
**Lesson:** a card's field list is a floor; when the card's *Why* and its *what-to-build* bullet disagree (here: "slots" vs "build correct SPARQL"), serve the Why — but source the extra detail from the code that already owns it (`store.py`'s predicate tables) so the new tool can't drift from what's actually persisted. And when a card asks to count something the store never wrote, name the missing capability instead of fabricating a number.

## 2026-06-19 — T69 (skill↔schema gate: quarantine-and-report, not hard-raise at load)
**What:** The card (§3) recommends the load-time fail mode be **raise** on a skill targeting a non-existent schema class. With the user, we softened to **quarantine-and-report**: such a skill is *not registered* in `SkillLoader` (so the agent can never extract with it) and emits a loud `warnings.warn`, but **construction does not raise** and the process keeps running. Membership stays hard (offline, the thing that's truly broken); unresolved EMMO/H2KG IRIs only warn, surfaced on demand by the `check_skill` tool (network, advisory) — never at load.
**Why:** T69 is deliberately scoped to the *detector* half; the user explicitly deferred the *corrector* (LLM proposes a synonym-remap or a `schema/exploratory.yaml` addition → the exact T69 gate re-verifies → escalate to a 2nd agent/human only if unresolved) to a follow-up (overlaps T70/T71). A hard import-time raise would kill the very agent the corrector needs alive to self-heal the skill — "refuse to *use*, not refuse to *boot*." Also kept the detector strict-exact on purpose: both sides (the skill's `targets:` and the schema) are our own controlled vocabulary, so fuzzy matching would *hide* drift and defeat the gate (the failure mode that caused F2). Paper-side synonym/unit variability is a separate LLM concern already handled in `extract.py`/`normalize.py`, not here.
**Verdict:** accepted (user-approved 2026-06-19). No code-enforced invariant weakened: the gate is read-only and €0; quarantine *strengthens* safety (a malformed skill becomes unusable rather than silently producing wrong/empty extractions); workspace-confinement / provenance-on-insert / the €-budget gate are untouched. Also a stricter-than-card IRI reading: warn on **any** unresolved external `emmo:`/`h2kg:` IRI (the card's literal is "class_uri OR close_mapping resolves") — the safe direction for a drift detector.
**Lesson:** when a card's fail-mode and a later-stated autonomy goal conflict, the load-time behavior has to serve the *system* the feature lives in: a self-healing agent can't hard-crash at import on the artifact it's meant to repair. Quarantine (unusable + loud) is the loud-failure the card wants without the boot-time foot-gun.

## 2026-06-19 — T71 (PEMWE-anode skill: +normalize.py/store.py beyond the card's "Will touch"; catalyst_loading slot left unmapped; OER comment stale)
**What:** Three notes on the second-domain skill. (1) **Schema additions touched `normalize.py` + `store.py`, which the card's "Will touch" (skills/schema/tests) does NOT list.** The user chose (2026-06-19) to add two Measurement classes (`PEMWECellVoltage`, `DegradationRate`) **plus** a `catalyst_loading` Condition slot. A new Measurement class needs a `UNIVERSAL_UNITS` entry (`normalize.py`) so its canonical unit is advertised to the LLM and `units_match` validates it (`extract.py:485` only checks when `canonical_unit()` is non-None — so a missing entry *skips* validation rather than dropping, but the T52 precedent + correctness want it present). A new **Condition slot must be added to `store.py:_COND_SCALARS`** or it validates in Pydantic and is **silently dropped at insert** — the exact T50 modeled-but-unpersisted trap; `test_t71_catalyst_loading_reaches_graph` pins it. Both touches are necessary, not drift; flagged here per the per-task convention. `extract.py`/`pipeline.py`/`agent.py`/`skills/oer-extraction/*` untouched (verified). (2) **`catalyst_loading` slot carries no `close_mapping`** even though `h2kg:CatalystLoading` exists. Its sibling Condition slots (`current_density`, `temperature_C`, …) carry no ontology mappings — only Measurement classes do, and the T69 gate checks only Measurement-class targets — so matching sibling style (CLAUDE.md §3) over a one-off slot mapping; the card's H2KG-alignment requirement is satisfied by the two new Measurement classes' close_mappings (`h2kg:CellVoltage`, `h2kg:CellVoltageIncreaseRate`, both verified to resolve as h2kg:Property individuals — close NOT exact, per T47). (3) **The OER skill's "PEMWE cell voltage … no schema class yet (T18a F3)" notes (`skills/oer-extraction/SKILL.md:8,46`) are now stale** — `PEMWECellVoltage` exists as of T71. Left untouched because the card forbids touching `oer-extraction`; recorded as a one-line follow-up (separate commit/task).
**Why:** Option A (2 classes + slot) was the user's grounded choice — all three quantities (cell voltage, voltage degradation rate, Ir loading) recur across the 4 PEMWE-anode corpus ground-truth files with no schema home, so the additions close a real gap (matching T18a F3, which deferred `PEMWECellVoltage` *only* because no paper then reported it). Nothing speculative.
**Verdict:** accepted (user-approved 2026-06-19; option A + attempt-live). No code-enforced invariant weakened: provenance-on-insert holds (new classes use the generic `insert_extraction` path; live run confirmed 4/4 measurements carry sha+parser+page+bbox); €-budget gate intact (offline edits, live extract metered at €0.0039); pipeline-only graph writes unchanged. Independent `reviewer` subagent: **PASS, 0 blocking**; the named test gap (multi-skill overlay must not shadow the now-universal `catalyst_loading`) was fixed (`test_pemwe_overlay_does_not_shadow_universal_keys`), not deferred.
**Lesson:** adding a schema *slot* is never just a schema edit — the persistence table (`_COND_SCALARS`) and the unit-advertisement table (`UNIVERSAL_UNITS`) are part of the same change, or the slot is modeled-but-dead (T50). The card's "Will touch" list was written before that coupling was visible; the honest move is to flag the extra touches, not pretend they fit.

## 2026-06-20 — T72 (Stage-1 prune: dropped ONLY Opus, kept the worst model, deferred the default-change)
**What:** The T72 plan's prune rule said "drop models dominated on `eur_per_tp` AND F1." Stage-1 (mineru, 5 papers) made the locked T50 runtime default **`deepseek-v4-flash` the worst model** (0.27 F1, 0% recall on 3/5 papers), and several others (`gpt-5.4`, `haiku-4.5`, `gpt-5.4-mini`) were cost/quality-dominated. With the user we **pruned only `opus-4.8`** and **kept every other model — including flash** — for the Stage-2 parser sweep. The locked `deepseek-v4-flash` default was **not** changed.
**Why:** Extraction accuracy is parser-CONDITIONAL (the thesis core); Stage-1 measured one parser. A model weak on mineru may be strong on docling/dots/paddle — flash's 0%-recall papers are partly a mineru text-coverage artifact (e.g. bd9811a5's figure-Tafel gold rows aren't in mineru's text at all, capping its recall at 4/8 for *everyone*). Pruning a model — least of all the runtime default — on a single parser's result would discard exactly the signal Stage-2 exists to capture. Opus is the one safe drop: it showed no accuracy edge over deepseek-pro/sonnet, so re-testing it across 4 parsers would only burn budget.
**Verdict:** accepted (user-approved 2026-06-20). No code-enforced invariant touched (experiment-only; €50 gate intact, run metered +€3.38 → €6.42). The default-model decision is **deferred, not made**. (Separately, a real budget-ledger bug was found and fixed en route — `palimpsest.db` was cwd-relative across CostMeter/ParserCache/ExtractionRunLog/viewer, so a subdir run forked an empty ledger; now one canonical repo-root path via `cost.canonical_db()`.)
**Lesson:** never re-point a locked runtime default on single-parser evidence, and don't prune a parser-conditional benchmark before the per-parser grid — drop only what is both dominated AND too costly to keep measuring (Opus). Strict json_schema is not free either: it net-hurt the cheap models (gemini 0.76→0.52, qwen 0.76→0.59 µf1), so "more constrained output" ≠ "better extraction" for small models.
